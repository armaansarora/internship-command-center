'use server';

export async function updateApplicationStatus(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function updateApplicationTier(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function updateApplicationNotes(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function createApplication(formData: FormData): Promise<{ error: string } | { success: true; id: number }> {
  throw new Error('Not implemented — awaiting Phase 1');
}
