'use server';

export async function createFollowUp(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function dismissFollowUp(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function completeFollowUp(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function snoozeFollowUp(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function updateFollowUpDate(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}
