'use server';

export async function createContact(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function updateContact(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function deleteContact(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}

export async function updateLastContacted(formData: FormData): Promise<{ error: string } | { success: true }> {
  throw new Error('Not implemented — awaiting Phase 1');
}
