paused: false
scope: R6-only
started: null
started_by: queued-by-partner-for-r6-kickoff
ended: null
ended_reason: null
max_blockers: 3
previous_phase: R5
previous_outcome: "R5 complete — 9/10 tasks (R5.4 live-compose streaming + pen-glow DEFERRED as B1/polish — PARTNER FLAGGED AS INTENT-LEVEL DRIFT, not true polish). 709 tests. tsc + build green. Lint 15e/11w (+4 warnings vs R4). Three-tone divergence is genuine (distinct system prompts, Jaccard<0.70 asserted). Private Supabase Storage enforced (admin client, no public URLs). Two-click approval gate (choose-tone → approve). Auth/Stripe untouched. R4's LinkedIn B1 carried forward."
next_phase: R6
next_brief: docs/NEXT-ROADMAP.md §10 R6 — The Briefing Room (Floor 3)
open_blockers_carrying_forward:
  - R4 B1 (LinkedIn OAuth env vars) — do not unblock without credentials
  - R5 B1 (R5.4 live-compose — partner decision pending whether to ship before R6)
verification_before_acceptance:
  - npm test
  - npx tsc --noEmit
  - npm run build
  - npm run lint
partner_constraints:
  - DO NOT repeat R5.4 pattern — Intent-level character flourishes are NEVER "polish." If the brief's Intent paragraph describes a behavior (live whiteboard fill, CPO mid-answer interruption), it ships or the phase does not mark acceptance.met.
  - tower verify ✗ is BINDING, not advisory — 9/10 tasks is not acceptance.met. If any verify check fails, do not flip the ledger.
  - Debrief Binder must render as a physical artifact on a shelf (ages visually over time); a JSON dump with a filename is an anti-pattern called out in the brief.
  - Voice recording is opt-in ONLY — private Supabase Storage, user can disable permanently; if opt-in enforcement cannot be guaranteed, ship text-only and open a blocker instead of shipping a half-gated voice path.
  - CEO/character voice has been deferred 3× (R2, R3, R4). R6 is a voice-candidate. Pick a clear outcome (ship voice OR explicitly defer with reason), no ambiguous partial.
