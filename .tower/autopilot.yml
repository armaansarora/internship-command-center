paused: true
scope: R10-only
started: 2026-04-23T21:30:00.000Z
started_by: sess-r10-autopilot
ended: 2026-04-24T04:52:00.000Z
ended_reason: "R10 scope complete — acceptance.met verified_by_commit d988f05. MVP shipped (R10.1–R10.12); R10.13/R10.14 stretch skipped per autopilot budget; R10.15 ledger-close run. Awaits user migration 0020 apply + FIRECRAWL_API_KEY env var for prod."
max_blockers: 3
previous_phase: R9
previous_outcome: "R9 complete — 11/11 tasks, acceptance.met (verified_by_commit: c31c658)."
current_phase: R10
next_brief: "docs/NEXT-ROADMAP.md — ### R10 — The Negotiation Parlor (C-Suite annex)"
verification_before_acceptance:
  - npm run t accept R10
partner_constraints:
  # ─── R10 pre-locked decisions (user, 2026-04-23, at autopilot trigger) ───
  - |
    (a) COMP BENCHMARKS — Firecrawl scrapes Levels.fyi into a
    `company_comp_bands` cache table, 30-day TTL. Firecrawl free tier is
    500 credits/month — cap the target list to the top ~20 companies per
    sector the user has touched, and cache aggressively. On Levels.fyi
    empty/miss: graceful-empty fallback (no pin, honest "not enough
    benchmark data" copy — not a fake band).
  - |
    (b) CEO VOICE ships here — the Parlor is the natural home for CEO
    voice. It reads negotiation drafts aloud (opt-in toggle, SAME
    three-layer pattern R6 voice uses — Settings toggle → per-surface
    enable check → graceful fallback when unavailable). Voice is never
    required.
  - |
    (c) SEND-HOLD — reuse R7's `send_after` column on outreach/negotiation
    emails for the 24h minimum hold on negotiation drafts. Do NOT rebuild
    scheduling infrastructure.
  - |
    (d) PDF — reuse R9's `@react-pdf/renderer` if any PDF artifact is
    needed (e.g., offer comparison export). Do NOT pick a second PDF lib.
  # ─── R10 non-negotiables ───
  - |
    PARLOR DOOR MATERIALIZATION — the Parlor door must be ABSENT from the
    DOM before the first offer parses. Not hidden. Not `display:none`.
    Not disabled. ABSENT. The CSuiteScene conditionally renders the door
    only when `offers.length > 0` is true (server-side gate). Before any
    offer exists, the C-Suite wall reads as a solid wood-panel wall with
    no door. After the first offer, the door materializes with a single
    cinematic beat.
  - |
    COMP CHART PINS — red below the 25th percentile, gold above the 75th,
    ink between. Multiple offers stack as folders on the oak table, and
    both (or all) pins appear simultaneously on the chart.
  - |
    OFFERS SCHEMA — migration 0020 with the exact columns named in the
    brief: base, bonus, equity, sign-on, housing, start_date, location,
    benefits jsonb, received_at, deadline_at, status enum. Plus: id (uuid
    PK), user_id (uuid FK → auth.users), application_id (uuid nullable
    FK → applications), created_at, updated_at. RLS `auth.uid() =
    user_id` on all four CRUD verbs.
  # ─── R10 scope ───
  - |
    SCOPE MVP (must-ship) — door-materialization + offers schema + comp
    chart + three-chair convening (Offer Evaluator + CFO + CNO) +
    negotiation-script drafting with send-hold.
  - |
    STRETCH (within phase, best-effort after MVP ships) — negotiation
    simulator (CPO as recruiter), CNO reference-request drafting.
  - |
    OUT OF SCOPE — §8 living expansions (post-offer transition layer,
    alumni floor, industry-cycle awareness, partner/spouse mode, moat
    layer). Not even stubbed.
  # ─── R10 anti-patterns (trip-wire against) ───
  - |
    ANTI-PATTERNS — do not ship: (i) a generic "Comp benchmarks" page,
    (ii) a "Negotiation templates" library, (iii) boilerplate email
    drafts, (iv) a door that exists before it should. Any of these = the
    build has drifted; back up.
