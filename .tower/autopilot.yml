paused: true
scope: R8-only
started: null
started_by: queued-by-partner-for-r8-kickoff
ended: null
ended_reason: null
max_blockers: 3
previous_phase: R7
previous_outcome: "R7 complete — 10/10 tasks, acceptance.met. 974 tests. Real undo via DB-level send_after predicate (P1 proof asserts Resend call count=0 inside undo window). Zero alert/toast anywhere. Quiet-hours server-side via computeDeliverAfter. Earned-arcs Situation Map. Auth/Stripe untouched. Post-R7 cleanup pass: drift detector now accepts split/bundled commits, tower accept command enforces verify-before-flip, autopilot.yml auto-advances scope on accept, lint back to 15e/0w (from 15e/19w), migration 005 written, env.example updated. 996 tests green."
next_phase: R8
next_brief: docs/NEXT-ROADMAP.md — ### R8 — The Rolodex Lounge (Floor 6)
open_blockers_carrying_forward:
  - R4 B1 (LinkedIn OAuth env vars) — do not unblock without credentials
  - R5 B1 (R5.4 live-compose streaming + pen-glow) — non-blocking for R8; fire R5.4 mini-phase separately
  - Migration 005 (src/db/manual/005_interview_audio_bucket.sql) — blocks R6 voice uploads in prod only
verification_before_acceptance:
  - npm run t accept R8
partner_constraints:
  - Use `npm run t accept R8` to flip acceptance.met — it enforces verify-before-flip structurally. Do NOT write acceptance.met:true directly to the YAML.
  - Consent UX is non-negotiable. Cross-user network features ship opt-in only, with explicit consent language and visible revoke. A Red Team read must pass. If consent surface can't be finished cleanly, ship rolodex-only and open a blocker for cross-user.
  - Rolodex rotates smoothly at 200+ cards. If chosen tech (R3F / CSS 3D / Lottie) can't hold that, drop to a smaller card budget and flag the ceiling — don't ship a demo that dies at scale.
  - Warmth decay must feel informative, not punitive. No red/warning visuals on cold cards; cool-blue is descriptive, not accusatory.
  - Do NOT repeat the R5.4 pattern. Intent-level character flourishes (cards cool physically, rolodex rotates on scroll, `[`/`]` side-switch, dossiers yellow and curl) are NOT polish — if they're in the Intent or Provocation lines, they ship.
  - Anti-patterns call-out: anything that reads as a CRM, contact list, LinkedIn embed, or uses the word "leads" is a fail.
