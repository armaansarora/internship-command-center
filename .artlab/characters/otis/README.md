# Otis Art Lab Status

Otis is now organized around the batch factory run `2026-05-14-otis-pilot`. Loose prototype folders were removed so future work cannot accidentally use stale files.

Use `ARTIFACTS.md` for the current labeled inventory. Otis has been promoted through the pipeline after Armaan's exact final approval phrase: `approved for app`.

Current warning: this pilot was built from prototype-sized transparent sources that were upscaled into 4K masters. The pipeline records that honestly in `run.json`; truly production-perfect Otis should eventually replace those sources with native high-resolution generation.
