# Mascot concept renders — drop GPT Image 2 outputs here

## ✅ LOCKED: the owl. See `MASCOT-DECISION.md`.
The mascot is **the owl**, using the two founder renders: **cream owl** = active mascot now (dark UI),
**navy owl** = light-mode twin. Same character, cream-on-dark / navy-on-light. Assets live at
`public/brand/owl-cream.png` and `public/brand/owl-navy.png`; component `src/components/identity/Mascot.tsx`;
live on `/lobby-pilot`. Everything below is the exploration history that led here.

## (history) The CUTE + FRIENDLY owl (5 different designs)
The founder chose the **cream owl** as the front-runner. The "elegant feminine" attempt read sultry, so the
direction corrected to **cute + friendly**, with each variation a **genuinely different design language**
(flat vector · chibi 3D · clay/vinyl · geometric Deco emblem · painterly). Active prompts:
**`CUTE-VARIATIONS.md`** (5 hardened one-shot prompts). Save those renders as
`cute-1-cheerbeak.png … cute-5-storybook.png` (variants `-a`, `-b`).

**Tip:** in a fresh GPT Image 2 session, attach `selected/cream-owl-favorite.png` as a reference (palette
anchor — these intentionally change her *form*), then paste one prompt.

`FEMININE-VARIATIONS.md` is the superseded sultry round, kept for history.

## Folders
- `selected/` — the founder's kept renders:
  - `cream-owl-favorite.png` — the front-runner (cream owl on a gold bar).
  - `navy-owl-keeper.png` — the dark owl on the gold keystone-tower (runner-up; more severe).
- `prompts/` — the prior round's per-prompt `.txt` files (owl-1…owl-6).

## Prior rounds (history)
- `OWL-VARIATIONS.md` — the 6 owl variations that produced the cream-owl winner.
- `../MASCOT-CONCEPTS.md` — the original 6 mascot concepts (tower / concierge / falcon / key / owl / seal).
