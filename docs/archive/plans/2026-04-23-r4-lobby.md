# R4 — The Lobby: Plan

Companion to `docs/plans/2026-04-23-r4-lobby-design.md`. Per-task TDD breakdown.

---

## R4.1 — Schema + migration

**Files:**
- `src/db/schema.ts` (add 5 columns to `userProfiles`)
- `src/db/migrations/0013_r4_lobby_onboarding.sql` (new)
- `src/db/migrations/meta/_journal.json` (touch)

**Tests first:** `src/db/schema.r4.test.ts` — a type-level assertion that `userProfiles.$inferSelect` has the 5 new fields with correct null/default semantics.

**Commit:** `[R4/4.1] feat(db): R4 lobby onboarding columns (arrival_played_at, concierge_target_profile, concierge_completed_at, first_briefing_shown, floors_unlocked)`

---

## R4.2 — Otis character primitives

**Files (new):**
- `src/components/lobby/concierge/OtisAvatar.tsx` — SVG silhouette (reception-vest figure). Burgundy accent.
- `src/components/lobby/concierge/OtisCharacter.tsx` — wraps avatar + reception-desk props (guest book, brass bell).
- `src/components/lobby/concierge/OtisDialoguePanel.tsx` — copy of `AgentDialoguePanel` pattern but with `accentClass="concierge-burgundy"` and rounder label typography.
- `docs/CHARACTER-PROMPTS.md` — replace `Name: TBD` with `Name: Otis` in the Concierge section and widen the voice rules.

**Tests first:**
- `src/components/lobby/concierge/OtisCharacter.test.tsx` — asserts (a) the SVG root has `data-character="otis"`, (b) the dialogue panel uses the burgundy accent class, (c) no import of any C-suite character component (grep-level).

**Commit:** `[R4/4.2] feat(lobby): Otis — Concierge character primitives (burgundy accent, reception desk)`

---

## R4.3 — Concierge conversation engine

**Files (new):**
- `src/lib/agents/concierge/system-prompt.ts` — Otis's system prompt + `OTIS_GREETING_BY_TIME(timezone)` pure helper.
- `src/lib/agents/concierge/target-profile-schema.ts` — Zod `targetProfileSchema`.
- `src/lib/agents/concierge/extract.ts` — wraps `generateObject` with schema, returns `{ profile, confidence }`.
- `src/app/api/concierge/chat/route.ts` — `streamText` with Otis system.
- `src/app/api/concierge/extract/route.ts` — calls `extract`, writes to `agent_memory` via `setTargetProfile` + to `user_profiles.concierge_target_profile` and stamps `concierge_completed_at`.

**Tests first:**
- `src/lib/agents/concierge/system-prompt.test.ts` — greeting varies by hour, no `Welcome back!`, no emoji except a single honorific "☞" allowed once, no startup-cringe words.
- `src/lib/agents/concierge/target-profile-schema.test.ts` — valid minimal / valid maximal / rejection on missing `roles`.
- `src/app/api/concierge/extract/route.test.ts` — skip-path produces `{ roles: ["Software Engineer"], seniority: "intern" }` placeholder and still writes the row.

**Commit:** `[R4/4.3] feat(ai): Concierge — streaming chat + structured target-profile extraction`

---

## R4.4 — Concierge conversation UI flow

**Files (new):**
- `src/components/lobby/concierge/ConcierceFlow.tsx` — the orchestrator: mounts Otis + dialogue panel + skip button, uses `useChat()` against `/api/concierge/chat`, fires `/api/concierge/extract` on completion or skip.
- `src/hooks/useConciergeFlow.ts` — pure state machine: `idle` → `greeting` → `collecting` → `confirming` → `extracting` → `done`.

**Tests first:**
- `src/hooks/useConciergeFlow.test.ts` — state-machine transitions under skip, under completion, under extraction failure.
- `src/components/lobby/concierge/ConcierceFlow.test.tsx` — renders Otis, skip button honored, extraction called on done.

**Commit:** `[R4/4.4] feat(lobby): Concierge flow — Otis conversation UI with skip honored`

---

## R4.5 — First-run Job Discovery trigger

**Files (new):**
- `src/app/api/onboarding/bootstrap-discovery/route.ts` — POST handler, session-auth (not cron-auth), awaits `runJobDiscoveryForUser(userId)` directly, returns result.
- `src/lib/onboarding/bootstrap.ts` — thin wrapper exported for tests + reuse.

**Tests first:**
- `src/app/api/onboarding/bootstrap-discovery/route.test.ts` — mocks the source fetch layer (`gatherCandidatesFromSources`), asserts the full pipeline runs (scoring, embeddings call, application inserts) and inserts ≥1 real application row for the test user.

**Commit:** `[R4/4.5] feat(onboarding): bootstrap-discovery — real first-run Job Discovery trigger`

---

## R4.6 — First-ever Morning Briefing override

**Files:**
- `src/lib/ai/agents/first-run-briefing.ts` (new) — thin wrapper around `generateMorningBriefing` that pulls the overnight snapshot from the user's just-inserted applications.
- `src/app/(authenticated)/penthouse/penthouse-data.ts` (edit) — if `first_briefing_shown = false` + `concierge_completed_at` within 10m → generate inline, atomic-update `first_briefing_shown = true`.
- `src/lib/db/queries/user-profile-rest.ts` (likely new or edit) — expose `shouldTriggerFirstBriefing(userId)` + `markFirstBriefingShown(userId)` with an atomic-update pattern.

**Tests first:**
- `src/lib/ai/agents/first-run-briefing.test.ts` — first-run path renders ≥3 beats referencing the discovered applications; subsequent mount doesn't re-render.
- `src/lib/db/queries/user-profile-rest.first-briefing.test.ts` — atomic update behavior: two concurrent callers, only one wins.

**Commit:** `[R4/4.6] feat(penthouse): first-run Morning Briefing override — references bootstrap-discovered jobs`

---

## R4.7 — Cinematic arrival scene (one-time-per-account)

**Files (new):**
- `src/components/lobby/cinematic/CinematicArrival.tsx` — the GSAP aerial approach + skip button.
- `src/components/lobby/cinematic/ArrivalStages.ts` — pure stage definitions (timings, easings) for unit tests.
- `src/lib/db/queries/user-profile-rest.ts` (edit) — `claimArrivalPlay(userId)` atomic update.

**Tests first:**
- `src/components/lobby/cinematic/ArrivalStages.test.ts` — stage sequencing deterministic, total duration ≤ 8 s, reduced-motion returns a single 0-duration stage.
- `src/lib/db/queries/user-profile-rest.arrival.test.ts` — atomic claim: returns true first time, false every subsequent call.

**Commit:** `[R4/4.7] feat(lobby): CinematicArrival — one-time-per-account aerial approach (CSS+GSAP)`

---

## R4.8 — Building Directory cross-section

**Files (new):**
- `src/components/lobby/directory/BuildingDirectory.tsx` — vertical stacked floors.
- `src/lib/lobby/derive-floors-unlocked.ts` — pure fn: `(activitySignal) => string[]` — derives floor unlock list.
- `src/lib/db/queries/user-profile-rest.ts` (edit) — `getFloorsUnlocked(userId)` + `syncFloorsUnlocked(userId)`.

**Tests first:**
- `src/lib/lobby/derive-floors-unlocked.test.ts` — 6 scenarios (no activity → `{L}`; apps only → `{L, 7}`; full active user → `{L, 1, 2, 3, 4, 5, 6, 7, PH}`).
- `src/components/lobby/directory/BuildingDirectory.test.tsx` — renders 9 rows, gold glow only on unlocked ones.

**Commit:** `[R4/4.8] feat(lobby): Building Directory — live cross-section of unlocked floors`

---

## R4.9 — Returning-user fast lane

**Files:**
- `src/proxy.ts` (edit) — when the request path is `/lobby` and the session is authenticated and `user_profiles.arrival_played_at IS NOT NULL` → redirect to `/<lastFloorVisited>`.
- `src/lib/supabase/middleware.ts` (edit) — already the session-aware path; add the fast-lane hook here.
- `src/app/api/floors/last-visited/route.ts` (new) — tiny POST to bump `lastFloorVisited`.
- `src/hooks/useTrackLastFloor.ts` (new) — client hook all authenticated floors mount; fires once per session.

**Tests first:**
- `src/lib/supabase/middleware.fast-lane.test.ts` — authenticated + arrival_played_at null → no redirect (first-visit still plays cinematic); authenticated + arrival_played_at non-null → redirect to `/<lastFloorVisited>`.
- `src/hooks/useTrackLastFloor.test.ts` — fires once per floor mount, debounced.

**Commit:** `[R4/4.9] feat(lobby): returning-user fast lane — skip lobby entirely when arrival is already played`

---

## R4.10 — LinkedIn OAuth blocker

**Action (no code):**
- `tower block R4.10 "LINKEDIN_CLIENT_ID/SECRET env vars not provisioned; LinkedIn sync deferred per autopilot directive — ship R4 without. Unblock when credentials land in .env.example + prod env."`
- Commit via tower CLI as a blocker-recording commit (tower handles the message).

**No UI trace** — R4.4's `ConcierceFlow` must not render a LinkedIn button or a "coming soon" line. This is a silent deferral.

---

## R4.11 — R4 Proof test

**Files (new):**
- `src/app/lobby/r4.proof.test.ts` — top-level integration test.

**Scenario:**
1. Create new user via Supabase admin.
2. Simulate `/api/concierge/chat` turn(s) → `/api/concierge/extract` with a sample Otis conversation.
3. Assert `user_profiles.concierge_target_profile` is non-null and `concierge_completed_at` stamped.
4. Call `/api/onboarding/bootstrap-discovery` → assert ≥1 application inserted with real `source_id`.
5. Simulate Penthouse data fetch → assert first-run briefing generated with ≥1 beat referencing the just-inserted application company.
6. Assert `first_briefing_shown = true`.

Source fetches stubbed via a fixture module; the *discovery pipeline* (scoring, dedupe, insert) is real.

**Commit:** `[R4/4.11] test(lobby): R4 Proof — end-to-end concierge → discovery → first briefing`

---

## R4.12 — R4 anti-pattern test

**Files (new):**
- `src/components/lobby/cinematic/CinematicArrival.second-visit.test.tsx` — mocks `arrival_played_at = ISO` → assert `CinematicArrival` returns null.
- `src/app/lobby/page.second-visit.test.ts` — mocks `arrival_played_at = ISO` server-side → assert fast-lane redirect via middleware mock.

**Commit:** `[R4/4.12] test(lobby): R4 anti-pattern — cinematic never plays twice`

---

## Ship

- `npm test` — all green.
- `npx tsc --noEmit` — zero errors.
- `npm run build` — green (this is the R0-regression catcher; do not skip).
- `npm run lint` — no new errors beyond the 15/6 baseline.
- For each completed task: `tower done R4.<n>`.
- `tower handoff --stdin` with JSON covering decisions, surprises, files, next.
- Push to origin.

---

## Parallelism plan

Subagent-driven candidates (no shared state across pairs):
- **Pair A:** R4.2 (Otis primitives) + R4.7 (cinematic) — different directories, no shared code.
- **Pair B:** R4.3 (conversation engine) + R4.8 (Building Directory) — different directories.
- **Pair C:** R4.5 (bootstrap-discovery) + R4.9 (fast lane) — different handlers.

R4.1 (schema) blocks everything — run it first, serially.
R4.4 (conversation UI) depends on R4.2 + R4.3 — runs after both.
R4.6 (briefing override) depends on R4.5 — runs after it.
R4.10 (blocker) is a no-code step — runs whenever.
R4.11 + R4.12 run last.
