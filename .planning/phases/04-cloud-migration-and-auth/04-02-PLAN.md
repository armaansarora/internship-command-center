---
phase: 04-cloud-migration-and-auth
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - internship-command-center/src/lib/dashboard.ts
  - internship-command-center/src/lib/follow-ups.ts
  - internship-command-center/src/lib/actions.ts
  - internship-command-center/src/lib/research.ts
  - internship-command-center/src/lib/follow-up-actions.ts
  - internship-command-center/src/lib/cover-letter-actions.ts
  - internship-command-center/src/app/page.tsx
  - internship-command-center/src/app/applications/page.tsx
  - internship-command-center/src/app/applications/[id]/page.tsx
  - internship-command-center/src/app/cover-letters/page.tsx
  - internship-command-center/src/app/follow-ups/page.tsx
autonomous: true
requirements: [CLOUD-02, CLOUD-05]

must_haves:
  truths:
    - "All 75+ applications visible on the tracker page"
    - "Dashboard loads with action items, status counts, and activity feed"
    - "Application detail pages render correctly with company research"
    - "Follow-ups page shows pending and suggested follow-ups"
    - "Cover letter generation still works end-to-end"
    - "All CRUD operations (add app, update status, create follow-up) still work"
  artifacts:
    - path: "internship-command-center/src/lib/dashboard.ts"
      provides: "Async dashboard data functions"
      contains: "async function getActionItems"
    - path: "internship-command-center/src/lib/follow-ups.ts"
      provides: "Async follow-up query functions"
      contains: "async function getPendingFollowUps"
    - path: "internship-command-center/src/app/page.tsx"
      provides: "Async dashboard page"
      contains: "await getActionItems"
  key_links:
    - from: "internship-command-center/src/app/page.tsx"
      to: "internship-command-center/src/lib/dashboard.ts"
      via: "await calls to async dashboard functions"
      pattern: "await (getActionItems|getStatusCounts|getRecentActivity)"
    - from: "internship-command-center/src/app/follow-ups/page.tsx"
      to: "internship-command-center/src/lib/follow-ups.ts"
      via: "await calls to async follow-up functions"
      pattern: "await (getPendingFollowUps|getSuggestedFollowUps)"
    - from: "internship-command-center/src/lib/actions.ts"
      to: "internship-command-center/src/db/index.ts"
      via: "await on db.update/insert.run()"
      pattern: "await db\\."
---

<objective>
Convert all database call sites from synchronous to async by adding `await` keywords and converting sync functions to `async`.

Purpose: After Plan 01 swapped the driver from better-sqlite3 (sync) to @libsql/client (async), every `.all()`, `.get()`, and `.run()` call now returns a Promise instead of a value. This plan mechanically updates all 11 files that call the database to properly await results.

Output: All existing features work identically against the async database driver. No sync better-sqlite3 patterns remain anywhere in the codebase.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cloud-migration-and-auth/04-RESEARCH.md

<interfaces>
<!-- Database module (rewritten in Plan 01) -->

From internship-command-center/src/db/index.ts (after Plan 01):
```typescript
import { createClient } from '@libsql/client';
import { drizzle } from 'drizzle-orm/libsql';
import * as schema from './schema';
export const db = globalThis.__db ?? createDb();
export { schema };
// All .all(), .get(), .run() calls now return Promise
```

From internship-command-center/src/db/schema.ts (UNCHANGED):
```typescript
export const applications = sqliteTable('applications', { ... });
export const companyResearch = sqliteTable('company_research', { ... });
export const followUps = sqliteTable('follow_ups', { ... });
export type Application, NewApplication, CompanyResearch, FollowUp;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert lib functions to async</name>
  <files>
    internship-command-center/src/lib/dashboard.ts,
    internship-command-center/src/lib/follow-ups.ts,
    internship-command-center/src/lib/actions.ts,
    internship-command-center/src/lib/research.ts,
    internship-command-center/src/lib/follow-up-actions.ts,
    internship-command-center/src/lib/cover-letter-actions.ts
  </files>
  <action>
Apply async migration to all lib files that call the database. Two categories:

**Category A: Functions that are SYNC and must become ASYNC (signature + await)**

`src/lib/dashboard.ts` — 3 functions:
- `getActionItems()` -> `async function getActionItems(): Promise<ActionItem[]>`
  - Line 47: `db.select().from(applications).all()` -> `await db.select().from(applications).all()`
  - Line 110-132: `db.select({...}).from(followUps).innerJoin(...).where(...).all()` -> add `await`
- `getStatusCounts()` -> `async function getStatusCounts(): Promise<StatusCounts>`
  - Line 168: `db.select().from(applications).all()` -> add `await`
- `getRecentActivity()` -> `async function getRecentActivity(): Promise<ActivityItem[]>`
  - Line 193-205: `db.select({...}).from(applications).orderBy(...).limit(10).all()` -> add `await` and wrap `return await ...`

`src/lib/follow-ups.ts` — 3 functions:
- `getPendingFollowUps()` -> `async function getPendingFollowUps(): Promise<FollowUpWithApp[]>`
  - The entire chained query ending in `.all()` needs `await`
- `getOverdueFollowUps()` -> `async function getOverdueFollowUps(): Promise<FollowUpWithApp[]>`
  - Same pattern
- `getSuggestedFollowUps()` -> `async function getSuggestedFollowUps(): Promise<Array<{...}>>`
  - Line 91-97: `db.select().from(applications).all()` -> add `await`
  - Line 100-107: `db.select({...}).from(followUps).where(...).all()` -> add `await`

**Category B: Functions ALREADY async — just add `await` before DB calls**

`src/lib/actions.ts` — 3 server actions (already `async`):
- `updateApplicationStatus`: `db.update(...).run()` -> `await db.update(...).run()`
- `updateApplicationNotes`: `db.update(...).run()` -> `await db.update(...).run()`
- `createApplication`: `db.insert(...).run()` -> `await db.insert(...).run()` — also verify `result.lastInsertRowid` still works (the existing `Number(result.lastInsertRowid)` cast should handle bigint from libSQL)

`src/lib/research.ts` — already `async` function `getCompanyResearch`:
- Line 87-91: `db.select().from(companyResearch).where(...).get()` -> add `await`
- Line 112-118: `db.update(companyResearch).set({...}).where(...).run()` -> add `await`
- Line 120-126: `db.insert(companyResearch).values({...}).run()` -> add `await`

`src/lib/follow-up-actions.ts` — 5 server actions (already `async`):
- `createFollowUp`: `db.insert(followUps).values({...}).run()` -> add `await`
- `dismissFollowUp`: `db.update(followUps).set({...}).where(...).run()` -> add `await`
- `completeFollowUp`: `db.update(followUps).set({...}).where(...).run()` -> add `await`
- `snoozeFollowUp`: `db.update(followUps).set({...}).where(...).run()` -> add `await`
- `updateFollowUpDate`: `db.update(followUps).set({...}).where(...).run()` -> add `await`

`src/lib/cover-letter-actions.ts` — already `async`:
- `getApplicationsForAutocomplete`: `db.select({...}).from(applications).all()` -> add `await`

IMPORTANT: Do NOT modify `src/lib/cover-letter.ts`, `src/lib/resume.ts`, `src/lib/tier-utils.ts`, or `src/lib/utils.ts` — these do not make database calls.
  </action>
  <verify>
    <automated>cd internship-command-center && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    - All 6 lib files updated with proper async/await
    - dashboard.ts: 3 functions now async with Promise return types
    - follow-ups.ts: 3 functions now async with Promise return types
    - actions.ts, research.ts, follow-up-actions.ts, cover-letter-actions.ts: all DB calls have `await`
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update page components for async data fetching</name>
  <files>
    internship-command-center/src/app/page.tsx,
    internship-command-center/src/app/applications/page.tsx,
    internship-command-center/src/app/applications/[id]/page.tsx,
    internship-command-center/src/app/cover-letters/page.tsx,
    internship-command-center/src/app/follow-ups/page.tsx
  </files>
  <action>
Update all page components to await async data functions. These are Next.js Server Components, so they can be `async` functions.

`src/app/page.tsx` (Dashboard):
- Change `export default function DashboardPage()` to `export default async function DashboardPage()`
- Add `await` before `getActionItems()`, `getStatusCounts()`, `getRecentActivity()`
  ```typescript
  const actionItems = await getActionItems();
  const statusCounts = await getStatusCounts();
  const recentActivity = await getRecentActivity();
  ```

`src/app/applications/page.tsx` (Tracker):
- Change `export default function ApplicationsPage()` to `export default async function ApplicationsPage()`
- Add `await` before `db.select().from(applications).orderBy(...).all()`
  ```typescript
  const allApps = await db.select().from(applications).orderBy(desc(applications.appliedAt)).all();
  ```

`src/app/applications/[id]/page.tsx` (Detail):
- Already `async` — just add `await` before `db.select().from(applications).where(...).get()`
  ```typescript
  const app = await db.select().from(applications).where(eq(applications.id, Number(id))).get();
  ```

`src/app/cover-letters/page.tsx`:
- Change `export default function CoverLettersPage()` to `export default async function CoverLettersPage()`
- Add `await` before `db.select({...}).from(applications).all()`
  ```typescript
  const allApps = await db.select({...}).from(applications).all();
  ```

`src/app/follow-ups/page.tsx`:
- Change `export default function FollowUpsPage()` to `export default async function FollowUpsPage()`
- Add `await` before `getPendingFollowUps()` and `getSuggestedFollowUps()`
  ```typescript
  const pending = await getPendingFollowUps();
  const suggestions = await getSuggestedFollowUps();
  ```

After all changes, run the full build to verify everything compiles and the app can start:
```bash
cd internship-command-center && npm run build
```

If TypeScript reports errors about missing `await` or Promise types, trace the error to the specific line and add the missing `await`.
  </action>
  <verify>
    <automated>cd internship-command-center && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 5 page components properly await async data functions
    - `npm run build` succeeds without errors
    - No synchronous database calls remain anywhere in the codebase
    - App is functionally identical to pre-migration (same data, same behavior, same UI)
  </done>
</task>

</tasks>

<verification>
1. `cd internship-command-center && npx tsc --noEmit` — zero TypeScript errors
2. `cd internship-command-center && npm run build` — production build succeeds
3. `cd internship-command-center && npx vitest run` — all tests pass
4. `grep -rn "\.all();\|\.get();\|\.run();" internship-command-center/src/lib/ internship-command-center/src/app/ --include="*.ts" --include="*.tsx" | grep -v "await" | grep -v "node_modules" | grep -v ".test."` — no un-awaited database calls (excluding test files which use @libsql/client directly)
5. `cd internship-command-center && npm run dev` — app starts and dashboard loads (manual spot-check)
</verification>

<success_criteria>
- Every `.all()`, `.get()`, `.run()` call in lib/ and app/ directories is preceded by `await`
- dashboard.ts, follow-ups.ts functions are `async` with `Promise<>` return types
- All page components that were sync are now `async`
- `npm run build` succeeds
- All existing vitest tests pass
- No runtime errors when navigating between pages (verified via build success)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cloud-migration-and-auth/04-02-SUMMARY.md`
</output>
