---
phase: 07-smarter-ai-and-networking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internship-command-center/src/db/schema.ts
  - internship-command-center/src/lib/cover-letter-actions.ts
  - internship-command-center/src/lib/cover-letter-versions.ts
  - internship-command-center/src/lib/interview-prep.ts
  - internship-command-center/src/lib/interview-prep-actions.ts
  - internship-command-center/src/components/cover-letters/version-history.tsx
  - internship-command-center/src/components/cover-letters/version-compare.tsx
  - internship-command-center/src/components/detail/interview-prep.tsx
  - internship-command-center/src/app/cover-letters/page.tsx
  - internship-command-center/src/app/applications/[id]/page.tsx
  - internship-command-center/src/__tests__/cover-letter-versions.test.ts
  - internship-command-center/src/__tests__/interview-prep.test.ts
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03

must_haves:
  truths:
    - "Every cover letter generation auto-saves to the database without requiring user action"
    - "Cover letter versions are browsable grouped by company on the /cover-letters page"
    - "User can compare two cover letter versions side-by-side in a simple two-column view"
    - "One cover letter per company can be marked as the 'active' version"
    - "Interview prep can be generated for any application with company overview, likely questions, talking points, and recent news"
    - "Interview prep is stored in DB and can be revisited without re-generating"
    - "Interview prep section appears on application detail page"
  artifacts:
    - path: "internship-command-center/src/db/schema.ts"
      provides: "coverLetters and interviewPrep tables"
      contains: "coverLetters"
    - path: "internship-command-center/src/lib/cover-letter-versions.ts"
      provides: "Version history queries and active marking"
      exports: ["getCoverLettersByCompany", "getAllCoverLettersGrouped", "setActiveCoverLetter"]
    - path: "internship-command-center/src/lib/interview-prep.ts"
      provides: "Interview prep generation and DB persistence"
      exports: ["generateInterviewPrep", "getInterviewPrep"]
    - path: "internship-command-center/src/components/cover-letters/version-history.tsx"
      provides: "Cover letter version list grouped by company"
      exports: ["VersionHistory"]
    - path: "internship-command-center/src/components/cover-letters/version-compare.tsx"
      provides: "Side-by-side comparison view"
      exports: ["VersionCompare"]
    - path: "internship-command-center/src/components/detail/interview-prep.tsx"
      provides: "Interview prep section for detail page"
      exports: ["InterviewPrepSection"]
  key_links:
    - from: "internship-command-center/src/lib/cover-letter-actions.ts"
      to: "internship-command-center/src/db/schema.ts"
      via: "db.insert(coverLetters) auto-save on generation"
      pattern: "coverLetters"
    - from: "internship-command-center/src/app/cover-letters/page.tsx"
      to: "internship-command-center/src/lib/cover-letter-versions.ts"
      via: "getAllCoverLettersGrouped() for version history"
      pattern: "getAllCoverLettersGrouped"
    - from: "internship-command-center/src/app/applications/[id]/page.tsx"
      to: "internship-command-center/src/lib/interview-prep.ts"
      via: "getInterviewPrep(applicationId) for detail page"
      pattern: "getInterviewPrep"
---

<objective>
Add cover letter versioning with auto-save, side-by-side comparison, active version pinning, and AI interview prep generation with DB persistence.

Purpose: Every cover letter generation is automatically preserved, enabling version comparison and "active" marking. Interview prep gives Armaan actionable prep materials (questions, talking points, news) stored for revisiting.
Output: coverLetters + interviewPrep DB tables, auto-save on generation, version history UI, comparison view, interview prep section on detail page
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-smarter-ai-and-networking/07-CONTEXT.md
@.planning/phases/07-smarter-ai-and-networking/07-RESEARCH.md

@internship-command-center/src/db/schema.ts
@internship-command-center/src/lib/cover-letter-actions.ts
@internship-command-center/src/lib/cover-letter.ts
@internship-command-center/src/lib/research.ts
@internship-command-center/src/lib/resume.ts
@internship-command-center/src/lib/follow-up-email-actions.ts
@internship-command-center/src/components/cover-letters/cover-letter-generator.tsx
@internship-command-center/src/app/cover-letters/page.tsx
@internship-command-center/src/app/applications/[id]/page.tsx
@internship-command-center/src/components/detail/company-research.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/cover-letter-actions.ts:
```typescript
export interface GenerationState {
  step: 'idle' | 'researching' | 'generating' | 'done' | 'error';
  content?: string;
  error?: string;
}

export async function generateCoverLetterAction(
  company: string,
  role: string,
): Promise<GenerationState>
// This function must be extended to auto-save to coverLetters table
```

From src/lib/research.ts:
```typescript
export async function getCompanyResearch(company: string): Promise<CompanyResearchData | null>
```

From src/lib/follow-up-email-actions.ts (Claude API call pattern):
```typescript
import Anthropic from '@anthropic-ai/sdk';
import { RESUME } from '@/lib/resume';
// const client = new Anthropic({ apiKey });
// const response = await client.messages.create({ model: 'claude-sonnet-4-20250514', ... });
```

From src/app/applications/[id]/page.tsx:
```typescript
// Server component that fetches app data, renders cards in a grid
// Interview prep section should be added as a new Card in the left column (md:col-span-2)
// Between the Notes card and the Email History card
```

From src/app/cover-letters/page.tsx:
```typescript
// Currently renders CoverLetterGenerator only
// Needs to add VersionHistory section below the generator
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover letter + interview prep schemas, auto-save, and data layer</name>
  <files>
    internship-command-center/src/db/schema.ts,
    internship-command-center/src/lib/cover-letter-actions.ts,
    internship-command-center/src/lib/cover-letter-versions.ts,
    internship-command-center/src/lib/interview-prep.ts,
    internship-command-center/src/lib/interview-prep-actions.ts,
    internship-command-center/src/__tests__/cover-letter-versions.test.ts,
    internship-command-center/src/__tests__/interview-prep.test.ts
  </files>
  <behavior>
    - Cover letter auto-save creates a DB record after generation with isActive=false
    - getAllCoverLettersGrouped() returns cover letters grouped by company name
    - setActiveCoverLetter(id) marks one version active and all others for that company inactive (transaction)
    - getCoverLettersByCompany(company) returns versions sorted by generatedAt desc
    - Interview prep record inserted with applicationId, content, generatedAt
    - getInterviewPrep(applicationId) returns latest prep for an application or null
  </behavior>
  <action>
    1. Add `coverLetters` table to `src/db/schema.ts` per RESEARCH.md schema:
       - id, applicationId (nullable FK to applications, onDelete: 'set null'), company, role, content, isActive (boolean default false), generatedAt (timestamp), createdAt (timestamp)
       - Export `CoverLetter` type

    2. Add `interviewPrep` table to `src/db/schema.ts` per RESEARCH.md schema:
       - id, applicationId (FK to applications, onDelete: 'cascade'), content, generatedAt (timestamp), createdAt (timestamp)
       - Export `InterviewPrep` type

    3. Run `npx drizzle-kit generate` then `npx drizzle-kit push` to create both tables.

    4. Modify `src/lib/cover-letter-actions.ts` — extend `generateCoverLetterAction`:
       - After successful generation (result.content exists), auto-save to coverLetters table
       - Accept optional `applicationId` parameter (number | undefined)
       - Insert with isActive: false (user explicitly marks active later)
       - Return the saved cover letter id in the response for downstream use
       - Do NOT change the existing generator flow — just add the save after generation

    5. Create `src/lib/cover-letter-versions.ts`:
       - `getAllCoverLettersGrouped()` — fetch all cover letters ordered by generatedAt desc, group by company in JS, return `Record<string, CoverLetter[]>`
       - `getCoverLettersByCompany(company: string)` — fetch cover letters for a company sorted by generatedAt desc
       - `getCoverLettersByApplication(applicationId: number)` — fetch cover letters for an application
       - `setActiveCoverLetter(id: number)` — in a transaction: (a) find the cover letter's company, (b) set all cover letters for that company to isActive=false, (c) set the target one to isActive=true. Use revalidatePath('/cover-letters').
       - `getActiveCoverLetter(company: string)` — fetch the active cover letter for a company or null

    6. Create `src/lib/interview-prep.ts`:
       - `generateInterviewPrep(company: string, role: string, applicationId: number)` — uses @anthropic-ai/sdk with Claude, includes Tavily research from getCompanyResearch(). System prompt per RESEARCH.md Pattern 5: structured output with "Company Overview", "Likely Questions", "Talking Points", "Recent News" sections. Uses RESUME data. Saves result to interviewPrep table. Returns the prep content.
       - `getInterviewPrep(applicationId: number)` — fetch latest prep for the application, return null if none

    7. Create `src/lib/interview-prep-actions.ts` — server actions:
       - `generateInterviewPrepAction(applicationId: number, company: string, role: string)` — calls generateInterviewPrep, returns { content } or { error }
       - `regenerateInterviewPrepAction(applicationId: number, company: string, role: string)` — same as generate but creates a NEW row (never overwrites). Per RESEARCH.md Pitfall 5: each generation creates a new row.

    8. Create test files:
       - `src/__tests__/cover-letter-versions.test.ts` — test auto-save creates record, getAllCoverLettersGrouped returns grouped data, setActiveCoverLetter transaction logic
       - `src/__tests__/interview-prep.test.ts` — test getInterviewPrep returns null when none exists, test insert and retrieval
  </action>
  <verify>
    <automated>cd internship-command-center && npx vitest run src/__tests__/cover-letter-versions.test.ts src/__tests__/interview-prep.test.ts -x</automated>
  </verify>
  <done>coverLetters and interviewPrep tables exist, generateCoverLetterAction auto-saves every generation, version queries group by company, active marking works with transaction, interview prep generates structured content and persists to DB, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Cover letter version history UI, comparison view, and interview prep section</name>
  <files>
    internship-command-center/src/components/cover-letters/version-history.tsx,
    internship-command-center/src/components/cover-letters/version-compare.tsx,
    internship-command-center/src/components/detail/interview-prep.tsx,
    internship-command-center/src/app/cover-letters/page.tsx,
    internship-command-center/src/app/applications/[id]/page.tsx
  </files>
  <action>
    1. Install shadcn tabs component: `npx shadcn@latest add tabs`

    2. Create `src/components/cover-letters/version-history.tsx` — 'use client' component:
       - Props: `{ versions: Record<string, CoverLetter[]> }` (grouped by company)
       - Renders companies as expandable sections (use Tabs or accordion-like collapsible sections)
       - Each version shows: generated date (formatDistanceToNow), role, truncated content preview (first 100 chars), "Active" badge if isActive
       - Buttons per version: "Set as Active" (calls setActiveCoverLetterAction server action, toast on success), "Compare" (checkbox-style selection, max 2)
       - When exactly 2 versions selected, show "Compare Selected" button that opens VersionCompare
       - Metadata visible per version: generated date, role — per CONTEXT.md specifics

    3. Create `src/components/cover-letters/version-compare.tsx` — 'use client' component:
       - Props: `{ versionA: CoverLetter, versionB: CoverLetter, onClose: () => void }`
       - Simple two-column layout in a Dialog (per CONTEXT.md: "simple two-column view, no diff highlighting")
       - Left column: Version A content with metadata header (company, role, date)
       - Right column: Version B content with metadata header
       - NO diff highlighting — just raw text side by side
       - Close button to dismiss the dialog

    4. Create server action wrapper for setActiveCoverLetter:
       - Add `setActiveCoverLetterAction` to `src/lib/cover-letter-actions.ts` (or a new file) that calls `setActiveCoverLetter(id)` from cover-letter-versions.ts. Use 'use server' directive.

    5. Update `src/app/cover-letters/page.tsx`:
       - Keep existing CoverLetterGenerator at top
       - Below it, fetch all cover letter versions via getAllCoverLettersGrouped()
       - Render VersionHistory component with the grouped versions
       - If no versions exist, show a subtle message: "Generated cover letters will appear here"
       - Use Tabs component: "Generator" tab (existing generator) and "Version History" tab (new)

    6. Create `src/components/detail/interview-prep.tsx` — 'use client' component:
       - Props: `{ applicationId: number, company: string, role: string, existingPrep: string | null }`
       - If existingPrep: render the prep content in structured sections (parse markdown headings for Company Overview, Likely Questions, Talking Points, Recent News). Use an expand/collapse pattern — collapsed by default showing "Interview Prep Ready" with expand button
       - If no prep: show a "Generate Interview Prep" button
       - "Re-generate" button (always visible when prep exists) calls regenerateInterviewPrepAction
       - Loading state with spinner during generation
       - Toast on success ("Interview prep generated") / error
       - Content formatted as bullet points, not paragraphs — per CONTEXT.md: "prepare in 5 minutes before the call"

    7. Update `src/app/applications/[id]/page.tsx`:
       - Import InterviewPrepSection and getInterviewPrep
       - Fetch interview prep: `const prep = await getInterviewPrep(Number(id))`
       - Add Interview Prep Card in the left column (md:col-span-2), between the Notes card and Email History card:
         ```
         <Card className="bg-card border-border">
           <CardHeader>
             <CardTitle className="text-base">Interview Prep</CardTitle>
           </CardHeader>
           <CardContent>
             <InterviewPrepSection applicationId={app.id} company={app.company} role={app.role} existingPrep={prep?.content ?? null} />
           </CardContent>
         </Card>
         ```
       - Show this card for ALL applications (not just interview status) — user might want to prep early
  </action>
  <verify>
    <automated>cd internship-command-center && npx vitest run --reporter=verbose && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Cover letters page shows generator + version history tabs, versions grouped by company with "Set as Active" and compare selection, side-by-side comparison dialog works with two-column layout, interview prep section appears on application detail page with generate/re-generate functionality, all tests pass, build succeeds</done>
</task>

</tasks>

<verification>
1. Generate a cover letter on /cover-letters — version auto-saved, appears in version history
2. Mark a version as active — "Active" badge appears, toast confirms
3. Select 2 versions and compare — side-by-side dialog shows both versions
4. Navigate to an application detail page — interview prep section visible
5. Generate interview prep — structured content with company overview, questions, talking points, news
6. Revisit the detail page — prep is loaded from DB without re-generating
7. `npx vitest run` — all tests pass
8. `npm run build` — no build errors
</verification>

<success_criteria>
- Every cover letter generation auto-saves to DB with isActive=false
- Version history shows all versions grouped by company with "Active" badge on pinned version
- Side-by-side comparison shows two versions in simple two-column layout (no diff highlighting)
- setActiveCoverLetter uses a transaction to ensure only one active per company
- Interview prep generates structured content (company overview, likely questions, talking points, recent news)
- Interview prep persists to DB and can be revisited without re-generation
- Re-generate creates a NEW row (never overwrites previous prep)
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-smarter-ai-and-networking/07-02-SUMMARY.md`
</output>
