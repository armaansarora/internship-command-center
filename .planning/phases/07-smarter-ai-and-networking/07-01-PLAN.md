---
phase: 07-smarter-ai-and-networking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internship-command-center/src/db/schema.ts
  - internship-command-center/src/lib/contacts.ts
  - internship-command-center/src/lib/contact-actions.ts
  - internship-command-center/src/components/contacts/warmth-badge.tsx
  - internship-command-center/src/components/contacts/contacts-columns.tsx
  - internship-command-center/src/components/contacts/contacts-table.tsx
  - internship-command-center/src/components/contacts/contact-form.tsx
  - internship-command-center/src/app/contacts/page.tsx
  - internship-command-center/src/app/contacts/loading.tsx
  - internship-command-center/src/components/layout/sidebar.tsx
  - internship-command-center/src/components/layout/bottom-tab-bar.tsx
  - internship-command-center/src/__tests__/contacts-schema.test.ts
  - internship-command-center/src/__tests__/warmth.test.ts
autonomous: true
requirements:
  - NET-01
  - NET-03
  - NET-04
  - NET-05
  - NET-06

must_haves:
  truths:
    - "Contacts table exists in DB with all fields including self-referential introduced_by FK"
    - "Contacts page shows a sortable/filterable table matching application tracker pattern"
    - "User can add a new contact via form with validation"
    - "Warmth badges display Hot/Warm/Cold based on lastContactedAt auto-decay"
    - "Contacts appear in sidebar and mobile bottom tab bar navigation"
    - "Contacts can be queried by company name for 'Who do I know?' functionality"
  artifacts:
    - path: "internship-command-center/src/db/schema.ts"
      provides: "contacts table with self-referential FK"
      contains: "contacts"
    - path: "internship-command-center/src/lib/contacts.ts"
      provides: "Contact CRUD queries and warmth computation"
      exports: ["getContacts", "getContactsByCompany", "computeWarmth"]
    - path: "internship-command-center/src/lib/contact-actions.ts"
      provides: "Server actions for contact mutations"
      exports: ["createContact", "updateContact", "deleteContact"]
    - path: "internship-command-center/src/app/contacts/page.tsx"
      provides: "Contacts list page"
      min_lines: 20
    - path: "internship-command-center/src/components/contacts/warmth-badge.tsx"
      provides: "Hot/Warm/Cold color-coded badge component"
      exports: ["WarmthBadge"]
  key_links:
    - from: "internship-command-center/src/app/contacts/page.tsx"
      to: "internship-command-center/src/lib/contacts.ts"
      via: "getContacts() call"
      pattern: "getContacts"
    - from: "internship-command-center/src/lib/contact-actions.ts"
      to: "internship-command-center/src/db/schema.ts"
      via: "db.insert(contacts) / db.update(contacts)"
      pattern: "contacts"
    - from: "internship-command-center/src/components/layout/sidebar.tsx"
      to: "/contacts"
      via: "navItems array entry"
      pattern: "contacts"
---

<objective>
Build the contacts/networking data layer, contacts page with table + form, warmth tracking, and navigation integration.

Purpose: Establishes the networking foundation that AI-powered features (detail page contact cards, company comparison) depend on. Armaan can manage contacts, see relationship warmth, and navigate to the contacts page.
Output: contacts DB table, contacts page with sortable table, add/edit form, warmth badges, navigation links
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-smarter-ai-and-networking/07-CONTEXT.md
@.planning/phases/07-smarter-ai-and-networking/07-RESEARCH.md

@internship-command-center/src/db/schema.ts
@internship-command-center/src/lib/actions.ts
@internship-command-center/src/components/applications/app-table.tsx
@internship-command-center/src/components/applications/columns.tsx
@internship-command-center/src/components/applications/status-badge.tsx
@internship-command-center/src/components/layout/sidebar.tsx
@internship-command-center/src/components/layout/bottom-tab-bar.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/db/schema.ts:
```typescript
export const applications = sqliteTable('applications', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  company: text('company').notNull(),
  // ... other fields
});
export type Application = typeof applications.$inferSelect;
```

From src/lib/actions.ts (server action pattern):
```typescript
'use server';
import { db } from '@/db';
import { applications } from '@/db/schema';
import { eq } from 'drizzle-orm';
import { revalidatePath } from 'next/cache';
import { z } from 'zod';
// Server actions use FormData input, z.safeParse validation, revalidatePath on success
```

From src/components/applications/columns.tsx (column definition pattern):
```typescript
import type { ColumnDef } from '@tanstack/react-table';
export const columns: ColumnDef<Application>[] = [
  { accessorKey: 'company', header: ({ column }) => <Button ...>, cell: ({ row }) => ... },
  // ... ArrowUpDown for sortable headers, inline Select for editable cells
];
```

From src/components/layout/sidebar.tsx:
```typescript
const navItems = [
  { href: '/', label: 'Overview', icon: LayoutDashboard },
  { href: '/applications', label: 'Applications', icon: Briefcase },
  { href: '/cover-letters', label: 'Cover Letter Lab', icon: FileText },
  { href: '/follow-ups', label: 'Follow-Ups', icon: Bell },
];
```

From src/components/layout/bottom-tab-bar.tsx:
```typescript
const navItems = [
  { href: '/', label: 'Home', icon: LayoutDashboard },
  { href: '/applications', label: 'Apps', icon: Briefcase },
  { href: '/cover-letters', label: 'Letters', icon: FileText },
  { href: '/follow-ups', label: 'Follow-Ups', icon: Bell },
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contacts schema, data layer, server actions, and navigation</name>
  <files>
    internship-command-center/src/db/schema.ts,
    internship-command-center/src/lib/contacts.ts,
    internship-command-center/src/lib/contact-actions.ts,
    internship-command-center/src/components/contacts/warmth-badge.tsx,
    internship-command-center/src/components/layout/sidebar.tsx,
    internship-command-center/src/components/layout/bottom-tab-bar.tsx,
    internship-command-center/src/__tests__/contacts-schema.test.ts,
    internship-command-center/src/__tests__/warmth.test.ts
  </files>
  <behavior>
    - contacts table inserts and reads correctly with all fields (name, company, email, phone, role, relationshipType, introducedBy, lastContactedAt, notes)
    - self-referential introduced_by FK allows linking contact A as referrer for contact B
    - getContactsByCompany('CompanyX') returns only contacts matching that company (case-insensitive)
    - computeWarmth(now) returns { level: 'hot', score ~100 }
    - computeWarmth(15 days ago) returns { level: 'warm' }
    - computeWarmth(31 days ago) returns { level: 'cold' }
    - computeWarmth(null) returns { level: 'cold', score: 0 }
  </behavior>
  <action>
    1. Add `contacts` table to `src/db/schema.ts` with all fields per RESEARCH.md Pattern 1:
       - id, name, company, email, phone, role, relationshipType (enum: recruiter/referral/alumni/cold_contact)
       - introducedBy (integer, nullable), lastContactedAt (timestamp, nullable), notes, createdAt, updatedAt
       - Use `foreignKey()` operator in third argument for self-referential FK (NOT inline .references())
       - Add indexes on company and lastContactedAt
       - Export `Contact` and `NewContact` types

    2. Run `npx drizzle-kit generate` then `npx drizzle-kit push` to create the table.

    3. Create `src/lib/contacts.ts` with:
       - `computeWarmth(lastContactedAt: Date | null)` — exponential decay per RESEARCH.md Pattern 2 (tau=13, hot<=7d, warm<=30d, cold>30d)
       - `getContacts()` — fetch all contacts, compute warmth for each, return sorted by warmth (hot first)
       - `getContactsByCompany(company: string)` — case-insensitive match using SQL LOWER()
       - `getContactById(id: number)` — single contact fetch
       - Export `WarmthLevel` type ('hot' | 'warm' | 'cold') and `ContactWithWarmth` type

    4. Create `src/lib/contact-actions.ts` with server actions following existing `actions.ts` pattern:
       - `createContact(formData: FormData)` — validates with zod schema, inserts, revalidatePath('/contacts')
       - `updateContact(formData: FormData)` — validates, updates by id, revalidatePath
       - `deleteContact(formData: FormData)` — deletes by id, revalidatePath
       - `updateLastContacted(formData: FormData)` — updates lastContactedAt to now for a contact

    5. Create `src/components/contacts/warmth-badge.tsx` — color-coded badge matching StatusBadge pattern:
       - Hot: emerald/green, Warm: amber/yellow, Cold: zinc/gray
       - Use Badge component with variant="outline" and cn() for conditional styles

    6. Update `src/components/layout/sidebar.tsx` — add `{ href: '/contacts', label: 'Contacts', icon: Users }` to navItems array (after Applications, before Cover Letter Lab). Import `Users` from lucide-react.

    7. Update `src/components/layout/bottom-tab-bar.tsx` — add `{ href: '/contacts', label: 'Contacts', icon: Users }` to navItems array (after Apps, before Letters). Import `Users` from lucide-react.

    8. Create test files:
       - `src/__tests__/contacts-schema.test.ts` — test insert, read, self-referential FK, getContactsByCompany
       - `src/__tests__/warmth.test.ts` — test computeWarmth with various dates (now, 15 days ago, 31 days ago, null)
  </action>
  <verify>
    <automated>cd internship-command-center && npx vitest run src/__tests__/contacts-schema.test.ts src/__tests__/warmth.test.ts -x</automated>
  </verify>
  <done>contacts table exists with self-referential FK, CRUD queries work, warmth computation returns correct levels, navigation updated in both sidebar and bottom tab bar, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Contacts page with table, form, and empty state</name>
  <files>
    internship-command-center/src/components/contacts/contacts-columns.tsx,
    internship-command-center/src/components/contacts/contacts-table.tsx,
    internship-command-center/src/components/contacts/contact-form.tsx,
    internship-command-center/src/app/contacts/page.tsx,
    internship-command-center/src/app/contacts/loading.tsx
  </files>
  <action>
    1. Create `src/components/contacts/contacts-columns.tsx` — column definitions for contacts table:
       - Columns: Name (sortable, bold), Company (sortable), Role, Relationship Type, Warmth (WarmthBadge), Last Contacted (formatDistanceToNow), Introduced By (link text or "--")
       - Follow exact pattern from `src/components/applications/columns.tsx` (ArrowUpDown for sortable headers)
       - Name column links to nothing (no detail page for contacts) but shows bold text
       - Warmth column uses WarmthBadge component
       - Relationship type column uses a styled badge (outline style, no gradient)

    2. Create `src/components/contacts/contacts-table.tsx` — reuse AppTable pattern:
       - Props: `{ data: ContactWithWarmth[] }`
       - useReactTable with getCoreRowModel, getSortedRowModel, getFilteredRowModel, getPaginationRowModel
       - SearchInput for global filter (search by name, company)
       - Same table chrome (rounded border, hover rows, pagination) as AppTable
       - Row click does NOT navigate anywhere (contacts have no detail page)

    3. Create `src/components/contacts/contact-form.tsx` — add/edit form:
       - 'use client' component using react-hook-form + zod validation
       - Fields: name (required), company (required), email (optional, email format), phone (optional), role (optional), relationshipType (required, select from 4 enum values), introducedBy (optional, select from existing contacts), notes (optional textarea)
       - Submit calls createContact or updateContact server action
       - Toast on success/error
       - Rendered inside a Sheet (slide-out panel) with trigger button "Add Contact"
       - For edit mode: receives optional `contact` prop to pre-fill form
       - Install shadcn sheet if not present: `npx shadcn@latest add sheet`

    4. Create `src/app/contacts/page.tsx` — server component:
       - Fetch contacts via getContacts()
       - Render page title "Contacts", subtitle "Your networking connections and referral chains"
       - "Add Contact" button (triggers Sheet with ContactForm)
       - If contacts exist: render ContactsTable
       - If empty: render EmptyState with Users icon, "No contacts yet", "Start building your network by adding recruiters, referrals, and alumni connections."
       - Action CTA: "Add Contact" (opens the form sheet)

    5. Create `src/app/contacts/loading.tsx` — skeleton matching page layout:
       - Mirror exact layout of contacts page (heading, button placeholder, table skeleton rows)
       - Use div with animate-pulse and bg-muted rounded blocks
  </action>
  <verify>
    <automated>cd internship-command-center && npx vitest run --reporter=verbose && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Contacts page renders at /contacts with sortable table showing warmth badges, add contact form validates and creates contacts with toast feedback, empty state shows when no contacts exist, loading skeleton prevents layout shift, all existing tests still pass, build succeeds</done>
</task>

</tasks>

<verification>
1. Navigate to /contacts — page loads with empty state or contacts table
2. Add a contact via the form — toast confirms, contact appears in table
3. Warmth badges show correct colors (Hot for recent, Cold for old/null lastContactedAt)
4. Sidebar and bottom tab bar show "Contacts" link
5. `npx vitest run` — all tests pass including new contacts-schema and warmth tests
6. `npm run build` — no build errors
</verification>

<success_criteria>
- contacts table exists in database with self-referential introduced_by FK
- /contacts page renders with sortable/filterable table matching application tracker style
- Add contact form validates input and creates contacts
- Warmth badges display Hot (green), Warm (amber), Cold (gray) based on auto-decay
- Contacts nav link appears in sidebar and mobile bottom tab bar
- getContactsByCompany returns contacts for a given company (case-insensitive)
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-smarter-ai-and-networking/07-01-SUMMARY.md`
</output>
