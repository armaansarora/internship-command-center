---
phase: 05-ui-ux-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internship-command-center/src/app/loading.tsx
  - internship-command-center/src/app/applications/loading.tsx
  - internship-command-center/src/app/applications/[id]/loading.tsx
  - internship-command-center/src/app/cover-letters/loading.tsx
  - internship-command-center/src/app/follow-ups/loading.tsx
  - internship-command-center/src/components/shared/empty-state.tsx
  - internship-command-center/src/app/page.tsx
  - internship-command-center/src/app/applications/page.tsx
  - internship-command-center/src/app/cover-letters/page.tsx
  - internship-command-center/src/app/follow-ups/page.tsx
autonomous: true
requirements: [UX-05, UX-06]

must_haves:
  truths:
    - "Every data-fetching page shows a skeleton layout (not blank screen or spinner) while loading"
    - "Skeleton layouts match the actual page dimensions to prevent layout shift"
    - "Empty data arrays show a helpful empty state with icon, message, and CTA button"
    - "Each empty state guides the user to take a specific action"
  artifacts:
    - path: "internship-command-center/src/app/loading.tsx"
      provides: "Dashboard loading skeleton"
    - path: "internship-command-center/src/app/applications/loading.tsx"
      provides: "Applications tracker table skeleton"
    - path: "internship-command-center/src/app/applications/[id]/loading.tsx"
      provides: "Application detail page skeleton"
    - path: "internship-command-center/src/app/cover-letters/loading.tsx"
      provides: "Cover letters page skeleton"
    - path: "internship-command-center/src/app/follow-ups/loading.tsx"
      provides: "Follow-ups page skeleton"
    - path: "internship-command-center/src/components/shared/empty-state.tsx"
      provides: "Reusable empty state component with icon, title, description, action"
      exports: ["EmptyState"]
  key_links:
    - from: "internship-command-center/src/app/loading.tsx"
      to: "internship-command-center/src/components/ui/skeleton.tsx"
      via: "Skeleton component usage for placeholder blocks"
      pattern: "<Skeleton"
    - from: "internship-command-center/src/app/page.tsx"
      to: "internship-command-center/src/components/shared/empty-state.tsx"
      via: "Conditional rendering when action items array is empty"
      pattern: "EmptyState"
---

<objective>
Create loading skeletons for all data-fetching routes and a reusable empty state component wired into every page.

Purpose: Loading skeletons prevent blank screens and layout shift during data fetching. Empty states guide users to take action instead of seeing confusing blank pages. Together these make the app feel polished and intentional.

Output: 5 loading.tsx skeleton files (one per route), 1 reusable EmptyState component, empty state conditionals in all page components.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-ux-overhaul/05-RESEARCH.md

<interfaces>
<!-- Existing page layouts that skeletons must mirror -->

From src/app/page.tsx (Dashboard):
```
<div className="p-6 max-w-[1200px] mx-auto space-y-8">
  header: h1 "Overview" + subtitle + QuickAddForm button (right)
  StatusCounters: row of count cards
  grid: 3-col on lg, 1-col on mobile
    - left 2 cols: "Needs Attention" h2 + ActionItems list
    - right col: "Recent Activity" h2 + bordered card with ActivityFeed
</div>
```

From src/app/applications/page.tsx (Tracker):
```
<div className="p-6 max-w-[1400px] mx-auto space-y-6">
  header: h1 "Applications" + count subtitle + QuickAddForm button
  AppTable: search input + filters row, then bordered table with ~10 rows visible
</div>
```

From src/app/applications/[id]/page.tsx (Detail):
- Need to check actual layout. Likely: company name heading, role, tier/status badges, notes section, follow-ups section, cover letters section

From src/app/cover-letters/page.tsx:
```
<div className="p-6 max-w-[800px] mx-auto space-y-6">
  header: h1 "Cover Letter Lab" + subtitle
  CoverLetterGenerator component
</div>
```

From src/app/follow-ups/page.tsx:
```
<div className="p-6 max-w-[900px] mx-auto space-y-8">
  header: h1 "Follow-Ups" + count subtitle
  sections: Overdue (red), Upcoming, Suggested
  each: list of FollowUpCard components
</div>
```

From src/components/ui/skeleton.tsx (EXISTS):
```typescript
export function Skeleton({ className, ...props }: React.HTMLAttributes<HTMLDivElement>)
// Renders a div with rounded-md animate-pulse bg-primary/10 classes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeletons for all 5 routes</name>
  <files>
    internship-command-center/src/app/loading.tsx,
    internship-command-center/src/app/applications/loading.tsx,
    internship-command-center/src/app/applications/[id]/loading.tsx,
    internship-command-center/src/app/cover-letters/loading.tsx,
    internship-command-center/src/app/follow-ups/loading.tsx
  </files>
  <action>
    Create a `loading.tsx` file in each route directory. Each file exports a default function that renders Skeleton components matching the actual page layout. Import `Skeleton` from `@/components/ui/skeleton`.

    CRITICAL: Each skeleton MUST mirror the exact padding, max-width, and grid structure of the corresponding page.tsx to prevent layout shift.

    1. **src/app/loading.tsx** (Dashboard skeleton):
       - `<div className="p-6 max-w-[1200px] mx-auto space-y-8">` (matches page.tsx exactly)
       - Header row: Skeleton h-8 w-48 (title), Skeleton h-4 w-32 mt-2 (subtitle), Skeleton h-9 w-36 (QuickAdd button)
       - Status counters: 6 Skeleton cards in a flex row (each h-20 w-full rounded-lg)
       - Grid: `grid grid-cols-1 lg:grid-cols-3 gap-6`
         - Left 2 cols: Skeleton h-6 w-40 (section title) + 4 Skeleton cards (h-20 w-full each, space-y-3)
         - Right col: Skeleton h-6 w-36 + bordered card with 5 Skeleton lines (h-4 varying widths)

    2. **src/app/applications/loading.tsx** (Tracker table skeleton):
       - `<div className="p-6 max-w-[1400px] mx-auto space-y-6">`
       - Header: Skeleton h-8 w-48, Skeleton h-4 w-32 mt-2, Skeleton h-9 w-36
       - Search/filter row: Skeleton h-9 w-72, 3x Skeleton h-9 w-24 (filter buttons)
       - Bordered table: 10 rows, each row is a flex with gap-4 p-4 border-b and Skeletons for company (w-32), role (w-48), tier (w-12), status (w-20), date (w-24)
       - Follow the exact pattern from 05-RESEARCH.md Pattern 5

    3. **src/app/applications/[id]/loading.tsx** (Detail page skeleton):
       - First read `src/app/applications/[id]/page.tsx` to understand the actual layout
       - Mirror: heading area (company name Skeleton h-8 w-64, role Skeleton h-5 w-48), badges row (2x Skeleton h-5 w-16), then grid with info card (5 rows of label+value Skeletons) and notes card (Skeleton h-32 textarea placeholder)
       - Include sections for follow-ups and cover letters if they appear on the detail page

    4. **src/app/cover-letters/loading.tsx** (Cover letters skeleton):
       - `<div className="p-6 max-w-[800px] mx-auto space-y-6">`
       - Header: Skeleton h-8 w-56, Skeleton h-4 w-64 mt-2
       - Generator area: Skeleton h-10 w-full (select dropdown), Skeleton h-10 w-32 (generate button), Skeleton h-64 w-full mt-4 (output area)

    5. **src/app/follow-ups/loading.tsx** (Follow-ups skeleton):
       - `<div className="p-6 max-w-[900px] mx-auto space-y-8">`
       - Header: Skeleton h-8 w-40, Skeleton h-4 w-48 mt-2
       - Section: Skeleton h-6 w-24 (section heading) + 3 cards (each h-28 w-full rounded-lg border border-border)
       - Second section: Skeleton h-6 w-28 + 3 more cards

    All loading.tsx files are plain React server components (no 'use client' needed). They export `default function Loading()`.
  </action>
  <verify>
    <automated>cd internship-command-center && ls src/app/loading.tsx src/app/applications/loading.tsx src/app/applications/\[id\]/loading.tsx src/app/cover-letters/loading.tsx src/app/follow-ups/loading.tsx && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 5 loading.tsx files exist (one per route)
    - Each skeleton mirrors the corresponding page layout (same padding, max-width, grid structure)
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Create EmptyState component and wire into all pages</name>
  <files>
    internship-command-center/src/components/shared/empty-state.tsx,
    internship-command-center/src/app/page.tsx,
    internship-command-center/src/app/applications/page.tsx,
    internship-command-center/src/app/cover-letters/page.tsx,
    internship-command-center/src/app/follow-ups/page.tsx
  </files>
  <action>
    1. Create `src/components/shared/empty-state.tsx`:
       - Follow the exact pattern from 05-RESEARCH.md "Reusable Empty State" code example
       - Props: `icon: LucideIcon`, `title: string`, `description: string`, `action?: { label: string; href: string }` (use `href` not `onClick` since these are server components — wrap the button in a Link)
       - Render: centered flex column with rounded-full bg-muted icon container, title (text-lg font-medium), description (text-sm text-muted-foreground max-w-sm), optional Button wrapped in Link
       - Note: Since page components are server components, use `href` on the action instead of `onClick`. The EmptyState component itself does not need 'use client'.

    2. Wire empty states into each page (add conditional rendering when data arrays are empty):

       **src/app/page.tsx** (Dashboard):
       - If `actionItems.length === 0`: Show EmptyState in the "Needs Attention" section with icon=`CheckCircle`, title="You're all caught up!", description="No items need your attention right now. Keep applying!", action={ label: "Browse Applications", href: "/applications" }
       - Import CheckCircle from lucide-react

       **src/app/applications/page.tsx** (Tracker):
       - If `allApps.length === 0`: Show EmptyState instead of AppTable with icon=`Briefcase`, title="No applications yet", description="Start tracking your internship applications to stay organized.", action={ label: "Add Application", href: "#" } (the QuickAddForm is already on the page — this is a fallback if the table is truly empty)
       - The existing AppTable already shows "No applications match your filters" for filtered-empty but has data. This empty state is for when there are zero applications total.

       **src/app/cover-letters/page.tsx** (Cover Letters):
       - If `allApps.length === 0`: Show EmptyState with icon=`FileText`, title="No applications to write for", description="Add some applications first, then come back to generate tailored cover letters.", action={ label: "Add Applications", href: "/applications" }
       - This handles the edge case where there are no apps to select from in the generator

       **src/app/follow-ups/page.tsx** (Follow-Ups):
       - The page already has a basic empty state text. REPLACE the existing `{pending.length === 0 && suggestions.length === 0 && ...}` block with the EmptyState component: icon=`Bell`, title="No follow-ups pending", description="You're all caught up! Follow-ups will appear here when you schedule them from application detail pages.", action={ label: "View Applications", href: "/applications" }
  </action>
  <verify>
    <automated>cd internship-command-center && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - EmptyState component exists at src/components/shared/empty-state.tsx
    - Dashboard shows "You're all caught up!" when no action items
    - Applications page shows empty state when no applications exist
    - Cover letters page shows empty state when no apps to generate for
    - Follow-ups page uses EmptyState component instead of plain text
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
1. `cd internship-command-center && npx next build` — full build passes
2. `cd internship-command-center && npx vitest run --reporter=verbose` — existing tests still pass
3. All 5 loading.tsx files exist
4. EmptyState component exists and is imported in all 4 page files
</verification>

<success_criteria>
- Every route has a loading.tsx that shows skeletons matching the page layout
- EmptyState component is reusable with icon, title, description, and optional CTA
- All pages render an empty state when their data arrays are empty
- No layout shift between skeleton and loaded content
- Build passes, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-ux-overhaul/05-02-SUMMARY.md`
</output>
