---
phase: 01-data-foundation-and-application-tracker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internship-command-center/package.json
  - internship-command-center/tsconfig.json
  - internship-command-center/next.config.ts
  - internship-command-center/drizzle.config.ts
  - internship-command-center/src/db/index.ts
  - internship-command-center/src/db/schema.ts
  - internship-command-center/src/db/seed.ts
  - internship-command-center/src/types/index.ts
  - internship-command-center/src/lib/resume.ts
  - internship-command-center/src/lib/utils.ts
  - internship-command-center/vitest.config.ts
  - internship-command-center/src/__tests__/db.test.ts
  - internship-command-center/src/__tests__/schema.test.ts
  - internship-command-center/src/__tests__/seed.test.ts
  - internship-command-center/src/__tests__/resume.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "SQLite database exists with WAL mode enabled and Drizzle ORM connects successfully"
    - "Application table stores all required fields: company, role, tier (T1-T4), sector, status, date applied, platform, contact info, notes"
    - "71+ applications are seeded from master data and queryable"
    - "Company research cache table exists with company_name (unique), research_json, fetched_at columns"
    - "Resume data is available as typed TypeScript constants with compile-time safety"
  artifacts:
    - path: "internship-command-center/src/db/index.ts"
      provides: "SQLite singleton with WAL mode"
      contains: "pragma.*journal_mode.*WAL"
    - path: "internship-command-center/src/db/schema.ts"
      provides: "Drizzle schema for applications, company_research, follow_ups"
      exports: ["applications", "companyResearch", "followUps"]
    - path: "internship-command-center/src/db/seed.ts"
      provides: "Seed script inserting 71+ applications"
      contains: "db.insert"
    - path: "internship-command-center/src/types/index.ts"
      provides: "Application, Status, Tier, Sector TypeScript types"
      exports: ["Application", "Status", "Tier", "Sector"]
    - path: "internship-command-center/src/lib/resume.ts"
      provides: "Armaan's resume as typed constants"
      exports: ["RESUME"]
  key_links:
    - from: "internship-command-center/src/db/index.ts"
      to: "internship-command-center/src/db/schema.ts"
      via: "import * as schema"
      pattern: "import.*schema"
    - from: "internship-command-center/src/db/seed.ts"
      to: "internship-command-center/src/db/schema.ts"
      via: "imports applications table"
      pattern: "applications.*from.*schema"
---

<objective>
Bootstrap the Next.js project, install all Phase 1 dependencies, create the SQLite database layer with Drizzle ORM, define the complete schema (applications, company_research, follow_ups), seed 71+ applications, create TypeScript types, and set up the resume data file. Also install Vitest and create test scaffolds for the data layer.

Purpose: Every other plan depends on this foundation -- the database, types, and project structure must exist first.
Output: A runnable Next.js project with a populated SQLite database, typed schema, and passing data layer tests.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-and-application-tracker/01-RESEARCH.md
@.planning/phases/01-data-foundation-and-application-tracker/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js project and install all Phase 1 dependencies</name>
  <files>
    internship-command-center/package.json,
    internship-command-center/tsconfig.json,
    internship-command-center/next.config.ts,
    internship-command-center/drizzle.config.ts,
    internship-command-center/vitest.config.ts,
    internship-command-center/src/lib/utils.ts
  </files>
  <action>
    1. Run `npx create-next-app@latest internship-command-center --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` from the workspace root (`/Users/armaanarora/Claude Code/`). Accept all defaults.

    2. `cd internship-command-center` and install dependencies:
       ```
       npm install drizzle-orm better-sqlite3 next-themes lucide-react clsx tailwind-merge class-variance-authority @tanstack/react-table react-hook-form zod @hookform/resolvers date-fns
       npm install -D drizzle-kit @types/better-sqlite3 tsx vitest @vitejs/plugin-react
       ```

    3. Initialize shadcn/ui: `npx shadcn@latest init` -- choose dark theme, CSS variables, src/components/ui directory. Then install Phase 1 components:
       ```
       npx shadcn@latest add table badge button dialog select command sheet card separator input textarea label tooltip dropdown-menu sidebar
       ```

    4. Create `drizzle.config.ts` at project root:
       ```typescript
       import { defineConfig } from 'drizzle-kit';
       export default defineConfig({
         out: './src/db/migrations',
         schema: './src/db/schema.ts',
         dialect: 'sqlite',
         dbCredentials: { url: process.env.DATABASE_PATH || './data/internship.db' },
       });
       ```

    5. Create `vitest.config.ts` at project root:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import react from '@vitejs/plugin-react';
       import path from 'path';
       export default defineConfig({
         plugins: [react()],
         test: { environment: 'node', globals: true },
         resolve: { alias: { '@': path.resolve(__dirname, './src') } },
       });
       ```

    6. Ensure `src/lib/utils.ts` has the `cn()` utility (shadcn init usually creates this, verify it exists with clsx + tailwind-merge).

    7. Create `data/` directory: `mkdir -p data` and add `data/` to `.gitignore`.
  </action>
  <verify>
    <automated>cd /Users/armaanarora/Claude\ Code/internship-command-center && npm run build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -3</automated>
  </verify>
  <done>Next.js project boots with `npm run dev`, all Phase 1 dependencies installed, shadcn components available, Vitest configured, drizzle.config.ts present, data/ directory exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create database schema, singleton, types, resume data, seed script, and tests</name>
  <files>
    internship-command-center/src/db/index.ts,
    internship-command-center/src/db/schema.ts,
    internship-command-center/src/db/seed.ts,
    internship-command-center/src/types/index.ts,
    internship-command-center/src/lib/resume.ts,
    internship-command-center/src/__tests__/db.test.ts,
    internship-command-center/src/__tests__/schema.test.ts,
    internship-command-center/src/__tests__/seed.test.ts,
    internship-command-center/src/__tests__/resume.test.ts
  </files>
  <behavior>
    - db.test.ts: Database connection is established, WAL mode is enabled (pragma returns 'wal'), singleton pattern works
    - schema.test.ts: applications table has all required columns (company, role, tier, sector, status, appliedAt, platform, contactName, contactEmail, contactRole, notes, createdAt, updatedAt); companyResearch table has companyName (unique), researchJson, fetchedAt; followUps table has applicationId FK, dueAt, completedAt, note, dismissed
    - seed.test.ts: After running seed, at least 71 applications exist in the database; seeded data includes all tiers (T1-T4) and multiple statuses
    - resume.test.ts: RESUME export exists and contains education (NYU Schack), experience (National Lecithin), GPA (3.58), major GPA (3.87), leadership, coursework arrays
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    1. Create `src/__tests__/db.test.ts` testing: WAL mode enabled, db instance exports correctly, singleton behavior.

    2. Create `src/__tests__/schema.test.ts` testing: all columns exist on applications, companyResearch, and followUps tables (use Drizzle schema introspection -- check that the column config objects have the expected keys).

    3. Create `src/__tests__/resume.test.ts` testing: RESUME constant has education.school === 'NYU Schack Institute of Real Estate', experience array includes National Lecithin, gpa === '3.58', majorGpa === '3.87'.

    4. Create `src/__tests__/seed.test.ts` testing: after seed function runs, db.select().from(applications).all() returns >= 71 rows, at least one row per tier.

    5. Run tests -- all should FAIL (RED).

    6. Implement `src/types/index.ts`:
       - Export type aliases: `Tier = 'T1' | 'T2' | 'T3' | 'T4'`
       - Export type: `Status = 'applied' | 'in_progress' | 'interview' | 'under_review' | 'rejected' | 'offer'`
       - Export type: `Sector = 'RE Finance' | 'Real Estate' | 'Finance' | 'Other'`
       - Export interface `Application` matching the Drizzle schema's select type (use `InferSelectModel<typeof applications>`)

    7. Implement `src/db/schema.ts` exactly as specified in the RESEARCH.md Pattern 2 -- applications table with all columns (company, role, tier as text enum, sector, status as text enum, appliedAt as integer timestamp, platform, contactName, contactEmail, contactRole, notes, createdAt, updatedAt), plus indexes on status, tier, company, appliedAt. Also companyResearch table (id, companyName unique, researchJson as json mode text, fetchedAt as timestamp) and followUps table (id, applicationId FK with cascade delete, dueAt, completedAt, note, dismissed as boolean).

    8. Implement `src/db/index.ts` using the singleton pattern from RESEARCH.md Pattern 1:
       - `const DB_PATH = process.env.DATABASE_PATH || './data/internship.db'`
       - `globalThis.__db` guard for hot reload
       - `sqlite.pragma('journal_mode = WAL')` and `sqlite.pragma('synchronous = NORMAL')`
       - Export `db = drizzle(sqlite, { schema })`
       - Also export a `getDb()` function that returns the db instance for testing (allows tests to use a separate in-memory or temp DB)

    9. Implement `src/lib/resume.ts`:
       - Export `RESUME` as a typed constant containing Armaan's data from PROJECT.md:
         - education: { school: 'NYU Schack Institute of Real Estate', degree: 'B.S. Real Estate', concentration: 'RE Finance', expectedGraduation: 'May 2028', gpa: '3.58', majorGpa: '3.87', honors: ["Dean's List"] }
         - experience: [{ company: 'National Lecithin', title: 'Summer Analyst Intern', period: 'May-Aug 2025', highlights: ['A/P and A/R operations', 'AI modernization initiative', 'Supplier negotiations'] }]
         - leadership: ['SREG Mentorship Program', 'Schack RE Club']
         - coursework: { completed: ['RE Finance', 'RE Accounting & Tax', 'RE Development', 'RE Law', 'Urban RE Economics'], current: ['Calculus I', 'Political Economy of Education', 'RE Finance', 'RE Development', 'RE Accounting & Tax'] }
         - writingStyle: { structure: '5-paragraph', tone: 'honest, grounded, specific', avoids: 'corporate buzzwords, fabricated facts' }

    10. Implement `src/db/seed.ts`:
        - Export an async `seed()` function
        - Insert 71+ application records using `db.insert(applications).values([...])` in batches
        - Data should represent a realistic internship search: mix of T1 (JPMorgan, Goldman Sachs, Blackstone, Brookfield -- RE Finance), T2 (CBRE, JLL, Cushman & Wakefield -- Real Estate), T3 (Morgan Stanley, Citi, Bank of America -- Finance), T4 (various other companies)
        - Include diverse statuses: most 'applied', some 'in_progress', a few 'interview' (including JPMorgan HireVue), some 'rejected', one 'under_review'
        - Include realistic dates (spread across Jan-Mar 2026), platforms (Handshake, LinkedIn, Company Website, Referral), contact info for key applications, and notes for T1/T2 applications
        - Add a runnable script entry: if `import.meta.url` matches process.argv, execute seed() directly so it can be run with `npx tsx src/db/seed.ts`

    11. Generate and run migrations: `npx drizzle-kit generate` then `npx drizzle-kit migrate`.

    12. Run the seed: `npx tsx src/db/seed.ts`

    13. Run tests -- all should PASS (GREEN).
  </action>
  <verify>
    <automated>cd /Users/armaanarora/Claude\ Code/internship-command-center && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All 4 test files pass. Database has 71+ seeded applications queryable via Drizzle. Schema covers applications (all fields), company_research (cache table), follow_ups (with FK). WAL mode confirmed. Resume data exports typed constants with Armaan's real data. Types file exports Application, Status, Tier, Sector.</done>
</task>

</tasks>

<verification>
1. `cd internship-command-center && npm run build` completes without errors
2. `npx vitest run --reporter=verbose` -- all tests pass
3. `npx drizzle-kit studio` opens and shows applications (71+ rows), company_research (0 rows), follow_ups (0 rows)
4. `node -e "const D = require('better-sqlite3'); const d = new D('./data/internship.db'); console.log(d.pragma('journal_mode')); console.log(d.prepare('SELECT COUNT(*) as n FROM applications').get())"` outputs WAL mode and count >= 71
</verification>

<success_criteria>
- Next.js project builds successfully
- All Vitest tests pass (db, schema, seed, resume)
- SQLite database exists at data/internship.db with WAL mode
- 71+ applications seeded with realistic data across all tiers and statuses
- TypeScript types export correctly
- Resume constants are compile-time safe
- Drizzle migrations generated and applied
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation-and-application-tracker/01-01-SUMMARY.md`
</output>
