---
phase: 01-data-foundation-and-application-tracker
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internship-command-center/src/app/layout.tsx
  - internship-command-center/src/app/globals.css
  - internship-command-center/src/app/page.tsx
  - internship-command-center/src/app/applications/page.tsx
  - internship-command-center/src/components/layout/theme-provider.tsx
  - internship-command-center/src/components/layout/sidebar.tsx
  - internship-command-center/src/components/applications/app-table.tsx
  - internship-command-center/src/components/applications/columns.tsx
  - internship-command-center/src/components/applications/status-badge.tsx
  - internship-command-center/src/components/applications/tier-badge.tsx
  - internship-command-center/src/components/applications/app-filters.tsx
  - internship-command-center/src/components/applications/search-input.tsx
autonomous: false
requirements:
  - TRACK-01
  - TRACK-02
  - TRACK-03
  - TRACK-09
  - TRACK-10
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07

must_haves:
  truths:
    - "App opens to dark-mode interface with slate/zinc palette, Inter font, generous whitespace"
    - "Left sidebar shows navigation with 4 sections: Overview, Applications, Cover Letter Lab, Follow-Ups"
    - "Applications page displays all 71+ seeded applications in a sortable table"
    - "User can sort by company name, tier, status, or date applied by clicking column headers"
    - "User can filter by tier (T1-T4), status, sector, and platform using dropdown selectors"
    - "User can search by company name or role title using a text input"
    - "Tier badges show correct colors: Gold for T1, Blue for T2, Violet for T3, Gray for T4"
    - "Status badges show correct colors: Emerald active/in_progress, Amber under_review, Fuchsia interview, Red rejected, Gray applied"
    - "Layout is responsive -- desktop-first but usable on tablet/mobile"
    - "Every interactive element has hover states and smooth transitions"
  artifacts:
    - path: "internship-command-center/src/app/layout.tsx"
      provides: "Root layout with dark mode, Inter font, ThemeProvider, sidebar"
      contains: "ThemeProvider"
    - path: "internship-command-center/src/app/applications/page.tsx"
      provides: "Application list Server Component querying all applications"
      contains: "db.select"
    - path: "internship-command-center/src/components/applications/app-table.tsx"
      provides: "TanStack Table wrapper with sorting, filtering, search"
      contains: "useReactTable"
    - path: "internship-command-center/src/components/applications/columns.tsx"
      provides: "Column definitions with sortable headers"
      exports: ["columns"]
    - path: "internship-command-center/src/components/applications/tier-badge.tsx"
      provides: "Color-coded tier indicator"
      contains: "amber-500.*blue-500.*violet-500.*zinc-500"
    - path: "internship-command-center/src/components/applications/status-badge.tsx"
      provides: "Color-coded status indicator"
      contains: "emerald-500.*amber-500.*fuchsia-500.*red-500"
    - path: "internship-command-center/src/components/layout/sidebar.tsx"
      provides: "Left sidebar navigation"
      contains: "Applications.*Cover Letter.*Follow-Ups"
  key_links:
    - from: "internship-command-center/src/app/applications/page.tsx"
      to: "internship-command-center/src/db/index.ts"
      via: "Server Component direct DB query"
      pattern: "db\\.select.*from.*applications"
    - from: "internship-command-center/src/components/applications/app-table.tsx"
      to: "internship-command-center/src/components/applications/columns.tsx"
      via: "column definitions import"
      pattern: "import.*columns"
    - from: "internship-command-center/src/app/layout.tsx"
      to: "internship-command-center/src/components/layout/sidebar.tsx"
      via: "sidebar rendered in root layout"
      pattern: "Sidebar"
---

<objective>
Build the complete dark-mode UI shell and the application tracker list view. The user opens the app and sees all 71+ applications in a sortable, filterable, searchable table with color-coded tier and status badges, within a clean dark-mode layout with sidebar navigation.

Purpose: This is the primary daily-driver view -- the first thing Armaan sees when opening the app.
Output: Working application list with full sort/filter/search, dark-mode shell, and navigation.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-and-application-tracker/01-RESEARCH.md
@.planning/phases/01-data-foundation-and-application-tracker/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From src/types/index.ts:
```typescript
export type Tier = 'T1' | 'T2' | 'T3' | 'T4';
export type Status = 'applied' | 'in_progress' | 'interview' | 'under_review' | 'rejected' | 'offer';
export type Sector = 'RE Finance' | 'Real Estate' | 'Finance' | 'Other';
export interface Application { /* Drizzle InferSelectModel<typeof applications> */ }
```

From src/db/index.ts:
```typescript
export const db: BetterSQLite3Database<typeof schema>;
```

From src/db/schema.ts:
```typescript
export const applications: SQLiteTable;
export const companyResearch: SQLiteTable;
export const followUps: SQLiteTable;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dark-mode UI shell with layout, navigation, and theme</name>
  <files>
    internship-command-center/src/app/layout.tsx,
    internship-command-center/src/app/globals.css,
    internship-command-center/src/app/page.tsx,
    internship-command-center/src/components/layout/theme-provider.tsx,
    internship-command-center/src/components/layout/sidebar.tsx
  </files>
  <action>
    1. Create `src/components/layout/theme-provider.tsx` as a client component wrapping `next-themes` ThemeProvider:
       ```typescript
       'use client';
       import { ThemeProvider as NextThemesProvider } from 'next-themes';
       import type { ThemeProviderProps } from 'next-themes';
       export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
         return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
       }
       ```

    2. Create `src/components/layout/sidebar.tsx` as a client component:
       - Left sidebar navigation with 4 sections: Overview (/), Applications (/applications), Cover Letter Lab (placeholder), Follow-Ups (placeholder)
       - Use Next.js `Link` and `usePathname()` for active state highlighting
       - Icons from lucide-react: LayoutDashboard (Overview), Briefcase (Applications), FileText (Cover Letter Lab), Bell (Follow-Ups)
       - Styling: `bg-zinc-900 border-r border-zinc-800`, width `w-64` on desktop, collapsible on mobile (use shadcn Sidebar component or custom implementation with responsive classes)
       - Active link: `bg-zinc-800 text-zinc-100`, inactive: `text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800/50`
       - App title at top: "Command Center" in `text-lg font-semibold text-zinc-100`
       - Cover Letter Lab and Follow-Ups nav items should link to their respective paths but show "Coming in Phase 2/3" placeholder text if those pages don't exist yet -- just make them regular Link elements
       - Every nav item must be a real clickable Link (UI-07)

    3. Update `src/app/layout.tsx`:
       - Import Inter font from `next/font/google` with `subsets: ['latin']`
       - Wrap children in ThemeProvider with `attribute="class"`, `defaultTheme="dark"`, `forcedTheme="dark"`
       - Outer div: `flex min-h-screen bg-zinc-950 text-zinc-100`
       - Render Sidebar on the left, main content in `<main className="flex-1 overflow-auto">`
       - Set metadata: title "Internship Command Center", description about tracking applications
       - Add `suppressHydrationWarning` to html element

    4. Update `src/app/globals.css`: ensure Tailwind v4 directives are present. Shadcn init should have set this up -- verify dark mode CSS variables are included.

    5. Update `src/app/page.tsx` (home page):
       - For Phase 1, redirect to /applications OR show a simple landing that says "Welcome to Internship Command Center" with a prominent link to "/applications"
       - Use: `import { redirect } from 'next/navigation'; export default function Home() { redirect('/applications'); }`
       - This will become the Dashboard in Phase 2

    6. Verify the app runs with `npm run dev` and shows the dark-mode shell with sidebar.
  </action>
  <verify>
    <automated>cd /Users/armaanarora/Claude\ Code/internship-command-center && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>App shows dark zinc-950 background with Inter font, left sidebar with 4 navigation items all clickable, and main content area. No white flash on load.</done>
</task>

<task type="auto">
  <name>Task 2: Build application tracker table with sorting, filtering, search, and badges</name>
  <files>
    internship-command-center/src/app/applications/page.tsx,
    internship-command-center/src/components/applications/app-table.tsx,
    internship-command-center/src/components/applications/columns.tsx,
    internship-command-center/src/components/applications/status-badge.tsx,
    internship-command-center/src/components/applications/tier-badge.tsx,
    internship-command-center/src/components/applications/app-filters.tsx,
    internship-command-center/src/components/applications/search-input.tsx
  </files>
  <action>
    1. Create `src/components/applications/tier-badge.tsx` exactly as specified in RESEARCH.md:
       - 'use client' component
       - Badge with variant="outline" and tier-specific colors:
         T1: `bg-amber-500/20 text-amber-400 border-amber-500/30` (Gold)
         T2: `bg-blue-500/20 text-blue-400 border-blue-500/30` (Blue)
         T3: `bg-violet-500/20 text-violet-400 border-violet-500/30` (Violet)
         T4: `bg-zinc-500/20 text-zinc-400 border-zinc-500/30` (Gray)

    2. Create `src/components/applications/status-badge.tsx` exactly as specified in RESEARCH.md:
       - 'use client' component
       - Badge with status-specific colors:
         applied: zinc (Gray), in_progress: emerald (Active), interview: fuchsia, under_review: amber, rejected: red, offer: emerald
       - Human-readable labels: 'Applied', 'In Progress', 'Interview', 'Under Review', 'Rejected', 'Offer'

    3. Create `src/components/applications/columns.tsx`:
       - 'use client' module exporting `columns: ColumnDef<Application>[]`
       - Columns: Company (sortable, renders as Link to /applications/[id]), Role, Tier (sortable, renders TierBadge), Status (sortable, renders StatusBadge), Applied (sortable, formatted with date-fns `format(date, 'MMM d, yyyy')` and relative time like "12d ago"), Platform, Sector
       - Sort headers use shadcn Button with ghost variant and ArrowUpDown icon from lucide-react
       - Company column: render as `<Link href={'/applications/' + row.original.id} className="text-zinc-100 hover:text-blue-400 hover:underline transition-colors">`
       - Handle null values in sorting: define custom sortingFn for date column that handles null/undefined

    4. Create `src/components/applications/search-input.tsx`:
       - 'use client' component accepting `value: string` and `onChange: (value: string) => void`
       - shadcn Input with Search icon from lucide-react
       - Placeholder: "Search companies or roles..."
       - Styling: `bg-zinc-900 border-zinc-700 text-zinc-100 placeholder:text-zinc-500`

    5. Create `src/components/applications/app-filters.tsx`:
       - 'use client' component that renders filter dropdowns for Tier, Status, Sector, and Platform
       - Use shadcn Select component for each filter
       - Each filter has an "All" option to clear the filter
       - Accept `columnFilters` state and `setColumnFilters` callback from parent
       - Tier options: T1, T2, T3, T4
       - Status options: Applied, In Progress, Interview, Under Review, Rejected, Offer
       - Sector options: RE Finance, Real Estate, Finance, Other
       - Platform options: derive from data or use common ones (Handshake, LinkedIn, Company Website, Referral, Other)
       - Layout: horizontal row of filter dropdowns with appropriate spacing

    6. Create `src/components/applications/app-table.tsx`:
       - 'use client' component -- this is the main table orchestrator
       - Props: `data: Application[]`
       - Uses `useReactTable` from @tanstack/react-table with:
         - `columns` from columns.tsx
         - `getCoreRowModel()`, `getSortedRowModel()`, `getFilteredRowModel()`, `getPaginationRowModel()`
         - State: `sorting`, `columnFilters`, `globalFilter` (for search)
       - Renders SearchInput at top, AppFilters below it, then shadcn Table component
       - Table rows should have `hover:bg-zinc-800/50 transition-colors cursor-pointer` and clicking a row navigates to `/applications/[id]` (use Next.js `useRouter`)
       - Show row count: "Showing X of Y applications"
       - If table has many rows, add basic pagination controls (Previous/Next buttons) at bottom
       - Empty state: "No applications match your filters" if filtered results are empty

    7. Create `src/app/applications/page.tsx` as a SERVER Component:
       - Import db and applications schema
       - Query all applications sorted by appliedAt descending: `const allApps = db.select().from(applications).orderBy(desc(applications.appliedAt)).all();`
       - Render page title "Applications" with subtitle showing total count
       - Render AppTable component passing the queried data
       - Add a "Quick Add" button placeholder (top right, next to title) -- just the button UI for now, functionality comes in Plan 03
       - Styling: padding p-6, generous whitespace (UI-03)

    8. Ensure all hover states have `transition-colors duration-150` or similar CSS transitions (UI-04). Use `cursor-pointer` on all clickable elements (UI-07).

    9. Test responsive behavior: table should have `overflow-x-auto` wrapper for mobile horizontal scrolling (UI-06).
  </action>
  <verify>
    <automated>cd /Users/armaanarora/Claude\ Code/internship-command-center && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Applications page shows all 71+ seeded records in a sortable table. Clicking Company/Tier/Status/Applied column headers toggles sort direction. Filter dropdowns narrow results by tier, status, sector, platform. Search input filters by company name or role. Tier badges are color-coded (Gold/Blue/Violet/Gray). Status badges are color-coded (Emerald/Amber/Fuchsia/Red/Gray). Table rows are clickable. Layout is responsive with horizontal scroll on mobile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dark-mode UI shell and application tracker</name>
  <files>None -- verification only</files>
  <action>
    Human verifies the complete dark-mode UI shell and application tracker table.
    What was built: Complete dark-mode UI shell with sidebar navigation and application tracker table showing 71+ seeded applications with sorting, filtering, search, and color-coded badges.
  </action>
  <verify>
    Steps to verify:
    1. Run `cd /Users/armaanarora/Claude\ Code/internship-command-center && npm run dev`
    2. Open http://localhost:3000 -- should redirect to /applications
    3. Verify dark mode: zinc-950 background, light text, no white flash
    4. Verify sidebar: 4 nav items (Overview, Applications, Cover Letter Lab, Follow-Ups), all clickable
    5. Verify table shows 71+ applications with Company, Role, Tier, Status, Applied, Platform, Sector columns
    6. Click "Company" header -- rows should sort alphabetically
    7. Click "Tier" header -- rows should sort T1-T4
    8. Click "Status" header -- rows should sort by status
    9. Click "Applied" header -- rows should sort by date
    10. Use tier dropdown to filter to T1 only -- should show ~10-15 gold-badged applications
    11. Use status dropdown to filter to "Interview" -- should show fuchsia-badged rows
    12. Type "JPMorgan" in search -- should filter to JPMorgan row(s)
    13. Verify tier badges: T1=Gold, T2=Blue, T3=Violet, T4=Gray
    14. Verify status badges: Applied=Gray, In Progress=Emerald, Interview=Fuchsia, Under Review=Amber, Rejected=Red
    15. Resize browser to mobile width -- table should scroll horizontally
    16. Check that hovering over rows and buttons shows visible hover state
  </verify>
  <done>Human approves the UI shell and tracker table -- dark mode, navigation, sorting, filtering, search, badges, and responsiveness all working correctly</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` shows the dark-mode app at localhost:3000
3. All 71+ applications visible and sortable
4. All filter combinations work without crashing
5. Search narrows results in real-time
6. Color-coded badges render correctly
7. Responsive layout works at all breakpoints
</verification>

<success_criteria>
- Dark-mode shell with Inter font, zinc palette, generous whitespace
- Sidebar navigation with 4 clickable sections
- Application table showing all seeded data
- All 4 sort columns work (company, tier, status, date)
- All 4 filter dimensions work (tier, status, sector, platform)
- Text search works for company name and role title
- Tier badges: Gold T1, Blue T2, Violet T3, Gray T4
- Status badges: Emerald active, Amber review, Fuchsia interview, Red rejected, Gray applied
- Responsive layout -- desktop-first, mobile-usable
- Smooth hover transitions on interactive elements
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation-and-application-tracker/01-02-SUMMARY.md`
</output>
