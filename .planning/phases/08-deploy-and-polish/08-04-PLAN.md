---
phase: 08-deploy-and-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 08-03
files_modified:
  - internship-command-center/src/app/page.tsx
  - internship-command-center/src/components/dashboard/attention-card.tsx
  - internship-command-center/src/components/dashboard/status-counters.tsx
  - internship-command-center/src/app/applications/page.tsx
  - internship-command-center/src/components/applications/application-card.tsx
  - internship-command-center/src/components/applications/card-grid-view.tsx
  - internship-command-center/src/components/applications/view-toggle.tsx
autonomous: true
requirements:
  - DEPLOY-04

must_haves:
  truths:
    - "Dashboard has greeting banner with gradient and hero stats at top"
    - "Dashboard cards are spaced out with visual accents, not a dense data dump"
    - "Tracker has both table and card grid view with a toggle to switch"
    - "Card grid view shows company info with visual richness (gradient accents, colored chips)"
    - "All pages use the PageHeader component from Plan 03"
    - "All raw <img> tags in modified components are migrated to next/image"
  artifacts:
    - path: "internship-command-center/src/app/page.tsx"
      provides: "Redesigned dashboard with greeting banner and hero stats"
      contains: "Welcome"
    - path: "internship-command-center/src/components/applications/card-grid-view.tsx"
      provides: "Card grid view for tracker applications"
      exports: ["CardGridView"]
    - path: "internship-command-center/src/components/applications/view-toggle.tsx"
      provides: "Toggle between table and card grid views"
      exports: ["ViewToggle"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/layout/page-header.tsx"
      via: "Uses PageHeader for gradient header"
      pattern: "PageHeader"
    - from: "src/app/applications/page.tsx"
      to: "src/components/applications/card-grid-view.tsx"
      via: "Renders card grid when view mode is cards"
      pattern: "CardGridView"
---

<objective>
Redesign the dashboard to feel like a "welcome home" screen and add a card grid view toggle to the tracker. Apply gradient headers and visual accents across both pages. Audit and migrate raw img tags to next/image per user decision.

Purpose: The two most-used pages (dashboard, tracker) get the premium visual treatment.
Output: Redesigned dashboard with greeting + hero stats, tracker with table/card toggle, next/image migration.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deploy-and-polish/08-CONTEXT.md
@.planning/phases/08-deploy-and-polish/08-RESEARCH.md
@.planning/phases/08-deploy-and-polish/08-03-SUMMARY.md

<interfaces>
From Plan 03 (will exist when this runs):
```typescript
// src/components/layout/page-header.tsx
export function PageHeader({ title, subtitle, children }: {
  title: string;
  subtitle?: string;
  children?: React.ReactNode;
}): JSX.Element;
```

From current dashboard (src/app/page.tsx):
- Server component that fetches action items, status counts, follow-ups, emails, calendar events
- Uses Promise.all() for parallel data fetching
- Renders StatusCounters, AttentionCard, FollowUpList, EmailWidget, CalendarWidget

From current tracker (src/app/applications/page.tsx):
- Server component with search, filter, sort parameters
- Renders application table with inline editing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign dashboard as "welcome home" screen and audit img tags</name>
  <files>
    internship-command-center/src/app/page.tsx,
    internship-command-center/src/components/dashboard/attention-card.tsx,
    internship-command-center/src/components/dashboard/status-counters.tsx
  </files>
  <action>
    1. Update src/app/page.tsx:
       - Add PageHeader at top with title "Welcome back, Armaan" (or greeting based on time of day: Good morning/afternoon/evening) and subtitle "Here's what needs your attention"
       - Hero stats section below header: 3-4 key numbers in large cards with gradient accents (total applications, active/in-progress count, interviews scheduled, follow-ups due). Use a grid layout (4 cols on lg, 2 on md, 1 on mobile).
       - Each hero stat card: large number, label below, subtle gradient left border in primary color, soft card shadow
       - Space out the existing sections (action items, follow-ups, email, calendar) with more padding between cards (gap-6 or gap-8 instead of compact stacking)
       - Add section headings with subtle dividers between major sections
       - Keep all existing data fetching and Promise.all() parallelization -- only change the presentation layer

    2. Restyle status-counters.tsx:
       - Instead of compact badges/pills, use visually rich cards with:
         - Gradient accent (left border or subtle background tint matching status color)
         - Larger count number
         - Status label below
         - Smooth hover effect (scale + shadow)

    3. Restyle attention-card.tsx:
       - Add soft card shadow and rounded corners
       - Subtle gradient left border based on urgency (warm = amber, urgent = red, standard = primary)
       - More breathing room (padding)
       - Keep existing functionality

    4. Audit and migrate raw img tags to next/image (per user decision "Use next/image where applicable"):
       - Search ALL components in src/ for raw <img> tags: grep -rn '<img ' src/
       - For each raw <img> found, replace with next/image import from 'next/image'
       - Set appropriate width/height or fill prop, add alt text if missing
       - For external URLs, add the domain to next.config.ts images.remotePatterns if needed
       - If no raw <img> tags exist, confirm audit was done (note in summary)
       - This covers the entire codebase, not just dashboard files
  </action>
  <verify>
    <automated>cd internship-command-center && grep -rn '<img ' src/ || echo "No raw img tags found - all migrated or none existed" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard has greeting banner, hero stats, spaced-out cards with gradient accents. All data fetching unchanged. All raw img tags across the codebase audited and migrated to next/image.</done>
</task>

<task type="auto">
  <name>Task 2: Add card grid view toggle to tracker</name>
  <files>
    internship-command-center/src/app/applications/page.tsx,
    internship-command-center/src/components/applications/card-grid-view.tsx,
    internship-command-center/src/components/applications/view-toggle.tsx,
    internship-command-center/src/components/applications/application-card.tsx
  </files>
  <action>
    1. Create src/components/applications/view-toggle.tsx:
       - Client component with "use client"
       - Two icons: LayoutGrid (card view) and Table2/List (table view) from lucide-react
       - Uses URL search params or local state to track view mode ("table" | "cards")
       - Styled as segmented control: two buttons, active one highlighted with primary background
       - Props: { view: "table" | "cards", onViewChange: (view) => void }

    2. Create src/components/applications/application-card.tsx:
       - Card component for a single application in grid view
       - Shows: company name (bold), role, tier badge (gradient, matching existing tier badge colors), status chip (colored), applied date, platform
       - Gradient left border or top accent based on tier
       - Soft shadow, rounded corners, hover scale effect
       - Click navigates to /applications/[id] detail page
       - Uses the Application type from schema
       - Use next/image for any company logos or images (per user decision)

    3. Create src/components/applications/card-grid-view.tsx:
       - Receives applications array
       - Renders responsive grid: 3 cols on lg, 2 on md, 1 on mobile
       - Maps over applications to render ApplicationCard
       - Stagger animation on mount (using motion library, matching existing patterns)

    4. Update src/app/applications/page.tsx:
       - Add PageHeader with title "Applications" and subtitle showing count
       - Add ViewToggle in the header area (as children prop of PageHeader)
       - Track view state via URL search param "view" (default: "table")
       - Conditionally render existing table OR new CardGridView based on view param
       - Soften existing table: lighter borders, subtle row hover with violet tint, remove harsh grid lines
       - Keep all existing search, filter, sort, inline editing functionality for table view
  </action>
  <verify>
    <automated>cd internship-command-center && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Tracker has table/card toggle. Card grid shows applications with visual richness. Table view softened. Both views are functional.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Dashboard renders greeting and hero stats
- Tracker /applications page accepts ?view=cards and ?view=table params
- CardGridView renders application cards in responsive grid
- No raw `<img>` tags remain in src/ (all use next/image)
</verification>

<success_criteria>
Dashboard feels like "welcome home" with greeting, hero stats, spaced cards. Tracker has both table and card grid views with toggle. All existing functionality preserved. All images use next/image per user decision.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-and-polish/08-04-SUMMARY.md`
</output>
