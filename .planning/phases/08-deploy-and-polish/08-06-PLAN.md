---
phase: 08-deploy-and-polish
plan: 06
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
  - 08-07
files_modified:
  - internship-command-center/README.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-05

user_setup:
  - service: vercel
    why: "Production hosting"
    env_vars:
      - name: TURSO_DATABASE_URL
        source: "From Plan 02 Turso setup (libsql:// URL)"
      - name: TURSO_AUTH_TOKEN
        source: "From Plan 02 Turso setup"
      - name: AUTH_SECRET
        source: "Generate with: npx auth secret"
      - name: AUTH_GOOGLE_ID
        source: "Google Cloud Console > Credentials > OAuth 2.0 Client ID"
      - name: AUTH_GOOGLE_SECRET
        source: "Google Cloud Console > Credentials > OAuth 2.0 Client Secret"
      - name: ALLOWED_EMAILS
        source: "Comma-separated: armaan.arora@nyu.edu,armaansarora20@gmail.com"
      - name: AUTH_TRUST_HOST
        source: "Set to: true"
      - name: ANTHROPIC_API_KEY
        source: "Anthropic dashboard > API Keys"
      - name: TAVILY_API_KEY
        source: "Tavily dashboard > API Keys"
      - name: NEXT_PUBLIC_SENTRY_DSN
        source: "Sentry project settings > Client Keys (DSN)"
      - name: SENTRY_DSN
        source: "Same DSN as above"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry > Settings > Auth Tokens"
    dashboard_config:
      - task: "Add Vercel production URL to Google Cloud Console OAuth redirect URIs"
        location: "Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client > Authorized redirect URIs > Add: https://<app>.vercel.app/api/auth/callback/google"
      - task: "Publish Google OAuth app from Testing to Production"
        location: "Google Cloud Console > APIs & Services > OAuth consent screen > Publish App"

must_haves:
  truths:
    - "App is deployed to Vercel and accessible via .vercel.app URL"
    - "GitHub pushes to main auto-deploy to production"
    - "Non-main branches get preview deploy URLs"
    - "All environment variables are configured in Vercel"
    - "Dashboard loads in under 2 seconds on production"
    - "Google OAuth sign-in works on production URL"
  artifacts:
    - path: "internship-command-center/README.md"
      provides: "Portfolio-quality README with project description and tech stack"
      contains: "Internship Command Center"
  key_links:
    - from: "GitHub main branch"
      to: "Vercel production"
      via: "Auto-deploy on push"
      pattern: "vercel.app"
    - from: "Vercel env vars"
      to: "src/auth.ts"
      via: "ALLOWED_EMAILS, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET"
      pattern: "process.env"
---

<objective>
Create GitHub repo, push all code, deploy to Vercel, configure all environment variables, write portfolio README, and verify production deployment.

Purpose: Ship the app to production. Accessible from any device via Vercel URL.
Output: Live production deployment, GitHub repo, portfolio README.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deploy-and-polish/08-CONTEXT.md
@.planning/phases/08-deploy-and-polish/08-RESEARCH.md
@.planning/phases/08-deploy-and-polish/08-01-SUMMARY.md
@.planning/phases/08-deploy-and-polish/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub repo, write README, run pre-deploy checks, and push</name>
  <files>internship-command-center/README.md</files>
  <action>
    1. Write portfolio-quality README.md in internship-command-center/:
       - Project title: "Internship Command Center"
       - Tagline: "Your internship command center -- track applications, generate cover letters, manage follow-ups, and nail your internship search."
       - Tech stack section: Next.js 16, Turso (SQLite), Drizzle ORM, Auth.js v5, Tailwind CSS v4, shadcn/ui, Claude AI, Tavily, Gmail API, Google Calendar API, Framer Motion, Sentry
       - Features section (bulleted): application tracker, AI cover letters, interview prep, company comparison, email integration, calendar events, contact networking, command palette, PWA
       - Screenshots section: placeholder markdown image references (user will add screenshots later)
       - Getting Started section: clone, npm install, copy .env.example, configure env vars, npm run dev
       - Architecture section: brief overview (Next.js App Router, server components, server actions, Turso cloud DB)
       - Include .planning/ reference: "See .planning/ directory for project documentation and planning artifacts"

    2. Run pre-deploy checks inside internship-command-center/:
       - npm run build (must succeed -- fix any errors)
       - npm run lint (must succeed -- fix any errors)
       - npx vitest run (run existing tests -- fix if any fail)

    3. Create fresh GitHub repo and push:
       - cd into internship-command-center/
       - Check if .git already exists. If the current directory is inside the parent "Claude Code" repo, we need a FRESH git init:
         a. Remove any existing .git (if it's a symlink or pointer to parent)
         b. git init
         c. git add -A
         d. Verify nothing sensitive is staged: git diff --cached --name-only | grep -E "\.env|\.db|\.sqlite" (should return nothing)
         e. git commit -m "Initial commit: Internship Command Center v2.0"
       - Create GitHub repo: gh repo create internship-command-center --public --source=. --remote=origin --push
       - If gh is not available, provide manual instructions as fallback

    4. Important: Include .planning/ directory in the push (per CONTEXT.md -- portfolio documentation).
       Do NOT include: .env, .env.local, data/*.db, node_modules, .next, .vercel
  </action>
  <verify>
    <automated>cd internship-command-center && git log --oneline -1 && gh repo view --json name 2>/dev/null | head -3 || echo "verify manually"</automated>
  </verify>
  <done>GitHub repo created and code pushed. README is portfolio-quality. Build, lint, and tests pass. No sensitive files committed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy to Vercel and configure production environment</name>
  <action>User deploys to Vercel, configures all env vars, updates Google OAuth redirect URIs, and verifies production deployment.</action>
  <what-built>Code pushed to GitHub. Now need Vercel deployment + Google OAuth production setup.</what-built>
  <how-to-verify>
    The user must perform these steps:

    **Step 1: Deploy to Vercel**
    Option A (CLI): Run from internship-command-center/:
      npx vercel --yes
      npx vercel --prod
    Option B (Dashboard): Visit vercel.com, import the internship-command-center repo, deploy.

    **Step 2: Configure Vercel settings**
    - Set region to iad1 (US East) in Vercel project settings > Functions > Region
    - Add ALL environment variables in Vercel dashboard > Settings > Environment Variables:
      * TURSO_DATABASE_URL = (production Turso URL from Plan 02)
      * TURSO_AUTH_TOKEN = (production Turso token from Plan 02)
      * AUTH_SECRET = (generate with: npx auth secret)
      * AUTH_GOOGLE_ID = (from Google Cloud Console)
      * AUTH_GOOGLE_SECRET = (from Google Cloud Console)
      * ALLOWED_EMAILS = armaan.arora@nyu.edu,armaansarora20@gmail.com
      * AUTH_TRUST_HOST = true
      * ANTHROPIC_API_KEY = (from Anthropic dashboard)
      * TAVILY_API_KEY = (from Tavily dashboard)
      * NEXT_PUBLIC_SENTRY_DSN = (from Sentry, optional)
      * SENTRY_DSN = (same as above, optional)
    - Trigger a redeploy after adding env vars

    **Step 3: Update Google OAuth**
    - Go to Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 Client
    - Add redirect URI: https://<your-app>.vercel.app/api/auth/callback/google
    - Go to OAuth consent screen > Publish App (change from Testing to Production)

    **Step 4: Verify production**
    - Visit https://<your-app>.vercel.app
    - Sign in with Google
    - Verify dashboard loads (should show real data from Turso production DB)
    - Check that dashboard loads in < 2 seconds
    - Test on mobile (verify PWA install prompt appears)

    **Step 5: Test preview deploys (DEPLOY-02)**
    - Create a test branch: git checkout -b test-preview && git commit --allow-empty -m "test" && git push -u origin test-preview
    - Verify Vercel creates a preview deployment URL
    - Delete the test branch after verifying
  </how-to-verify>
  <verify>User confirms production URL is live and sign-in works</verify>
  <done>App deployed to Vercel, all env vars configured, Google OAuth works in production, dashboard loads fast, preview deploys functional</done>
  <resume-signal>Paste the production Vercel URL (e.g., https://internship-command-center.vercel.app) and confirm sign-in works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- GitHub repo exists and is public
- Vercel deployment is live
- Production URL loads dashboard
- Google OAuth sign-in works on production
- Dashboard loads in < 2 seconds
- Preview deploys work on non-main branches
</verification>

<success_criteria>
App is deployed to Vercel, accessible from any device via .vercel.app URL. Auto-deploy from main branch works. All env vars configured. Google OAuth works in production. Dashboard loads fast.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-and-polish/08-06-SUMMARY.md`
</output>
