---
phase: 08-deploy-and-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internship-command-center/src/app/globals.css
  - internship-command-center/src/app/layout.tsx
  - internship-command-center/src/app/sign-in/page.tsx
  - internship-command-center/src/components/layout/sidebar.tsx
  - internship-command-center/src/components/layout/page-header.tsx
autonomous: true
requirements:
  - DEPLOY-04

must_haves:
  truths:
    - "App defaults to light mode with blue-violet accent palette"
    - "Dark mode still works via toggle (not broken)"
    - "Sign-in page has branded appearance with logo, tagline, and gradient"
    - "Every page has a gradient header area with title"
    - "Sidebar has Vercel/Stripe-inspired premium styling"
  artifacts:
    - path: "internship-command-center/src/app/globals.css"
      provides: "Blue-violet color palette for both light and dark modes"
      contains: "oklch"
    - path: "internship-command-center/src/components/layout/page-header.tsx"
      provides: "Reusable gradient page header component"
      exports: ["PageHeader"]
    - path: "internship-command-center/src/app/sign-in/page.tsx"
      provides: "Branded sign-in page with gradient background"
      contains: "Sign in with Google"
  key_links:
    - from: "src/app/layout.tsx"
      to: "globals.css"
      via: "CSS variables consumed by Tailwind"
      pattern: "defaultTheme.*light"
    - from: "src/components/layout/page-header.tsx"
      to: "globals.css"
      via: "Uses primary/accent CSS variables for gradient"
      pattern: "bg-gradient"
---

<objective>
Transform the app's visual identity from dark/techy to Vercel/Stripe-inspired elegance. Swap CSS palette to blue-violet, enable light mode as default, create gradient page headers, restyle sidebar, and redesign sign-in page.

Purpose: The visual foundation that all page-level redesigns build on. Every subsequent page inherits the new palette and components.
Output: New color system, light mode default, PageHeader component, restyled sidebar, branded sign-in.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deploy-and-polish/08-CONTEXT.md
@.planning/phases/08-deploy-and-polish/08-RESEARCH.md

<interfaces>
From internship-command-center/src/app/layout.tsx:
```typescript
// Current state -- must change:
<html lang="en" className="dark" suppressHydrationWarning>
<ThemeProvider attribute="class" defaultTheme="dark" forcedTheme="dark" disableTransitionOnChange>
<Toaster theme="dark" richColors closeButton />
```

From internship-command-center/src/app/globals.css:
```css
/* Current: neutral oklch palette with no hue. Must swap to blue-violet (hue ~275) */
:root { --primary: oklch(0.205 0 0); ... }
.dark { --primary: oklch(0.922 0 0); ... }
```

From internship-command-center/src/components/layout/sidebar.tsx:
- Uses shadcn Sidebar component with SidebarProvider, SidebarMenu, etc.
- Has navigation links and footer prop for SignOutButton

From internship-command-center/src/app/sign-in/page.tsx:
- Current sign-in page (needs branded redesign)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap color palette to blue-violet and enable light mode default</name>
  <files>
    internship-command-center/src/app/globals.css,
    internship-command-center/src/app/layout.tsx
  </files>
  <action>
    1. Update globals.css :root (light mode) palette:
       - Replace the neutral (hue 0) palette with blue-violet tones (hue ~275)
       - --background: near-white with slight violet tint (oklch ~0.985 0.002 280)
       - --foreground: near-black (oklch ~0.15 0.02 280)
       - --primary: bold blue-violet (oklch ~0.55 0.25 275) -- this is the brand accent
       - --primary-foreground: white (oklch ~0.98 0 0)
       - --secondary: soft violet tint (oklch ~0.96 0.01 280)
       - --muted: light violet gray (oklch ~0.96 0.008 280)
       - --muted-foreground: medium gray with violet (oklch ~0.55 0.02 280)
       - --accent: light violet (oklch ~0.95 0.02 275)
       - --card: white (oklch ~0.995 0.001 280)
       - --card-foreground: same as foreground
       - --border: soft violet border (oklch ~0.92 0.01 280)
       - --ring: violet focus ring (oklch ~0.55 0.2 275)
       - --sidebar colors: match the new palette with slightly deeper backgrounds
       - Keep chart colors but shift to violet/blue spectrum

    2. Update globals.css .dark palette:
       - --background: deep violet-black (oklch ~0.13 0.02 280)
       - --foreground: near-white
       - --primary: lighter blue-violet for dark mode (oklch ~0.65 0.25 275)
       - Adjust all other dark mode vars to complement the violet palette
       - Sidebar dark: slightly lighter than background

    3. Add utility gradient classes at the end of globals.css (before @layer base):
       ```css
       .gradient-brand {
         background: linear-gradient(135deg, oklch(0.55 0.25 275), oklch(0.55 0.22 300));
       }
       .gradient-header {
         background: linear-gradient(135deg, oklch(0.96 0.03 275), oklch(0.98 0.01 300));
       }
       .dark .gradient-header {
         background: linear-gradient(135deg, oklch(0.2 0.04 275), oklch(0.18 0.03 300));
       }
       ```

    4. Update layout.tsx:
       - Remove className="dark" from <html> tag (keep suppressHydrationWarning)
       - Change ThemeProvider: remove forcedTheme="dark", change defaultTheme to "light"
       - Change <Toaster theme="dark"> to <Toaster theme="system">
       - Keep everything else (session check, sidebar, LayoutTransition, etc.)
  </action>
  <verify>
    <automated>cd internship-command-center && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>App loads in light mode by default with blue-violet palette. Dark mode toggle still works. Toaster follows system theme.</done>
</task>

<task type="auto">
  <name>Task 2: Create PageHeader component, restyle sidebar, redesign sign-in page</name>
  <files>
    internship-command-center/src/components/layout/page-header.tsx,
    internship-command-center/src/components/layout/sidebar.tsx,
    internship-command-center/src/app/sign-in/page.tsx
  </files>
  <action>
    1. Create src/components/layout/page-header.tsx:
       - Export PageHeader component with props: { title: string, subtitle?: string, children?: ReactNode }
       - Renders a gradient header area using the gradient-header utility class
       - Title in large semibold text, subtitle in muted-foreground below
       - children slot for action buttons (right-aligned)
       - Padding: px-6 py-8 on desktop, px-4 py-6 on mobile
       - Subtle bottom border
       - Add decorative background shapes: absolute-positioned circles/blobs with low opacity (using primary color at ~5-10% opacity) for visual richness per CONTEXT.md

    2. Restyle sidebar.tsx:
       - Update to Vercel/Stripe-inspired look: clean lines, subtle borders, violet accent on active item
       - Active nav item: left accent border in primary color (blue-violet) + light primary background tint
       - Hover: soft violet background
       - Logo/brand area at top: app name "ICC" or "Internship Command Center" with small gradient accent
       - Keep existing nav structure (links, icons) -- just restyle
       - Add theme toggle button (sun/moon icon from lucide-react) in sidebar footer area, above the SignOutButton

    3. Redesign sign-in page (src/app/sign-in/page.tsx):
       - Full-screen centered layout with gradient background (gradient-brand or subtle variation)
       - Centered card with: app name "Internship Command Center", tagline "Your internship command center" (per CONTEXT.md), Google sign-in button
       - App logo area: use a gradient icon/badge or styled text with gradient
       - "Sign in with Google" button styled with clean white/light background, Google icon (or just text), rounded, shadow
       - Decorative background shapes (blobs/circles at low opacity) for visual depth
       - Professional first impression -- this is what people see first
  </action>
  <verify>
    <automated>cd internship-command-center && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>PageHeader component exists and is reusable. Sidebar has premium Vercel-inspired styling with theme toggle. Sign-in page is branded with gradient background and tagline.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- globals.css has blue-violet oklch values (not neutral gray)
- layout.tsx has defaultTheme="light" and no forcedTheme
- PageHeader component is exported and importable
- Sign-in page has "Your internship command center" tagline
</verification>

<success_criteria>
Visual foundation complete: blue-violet palette active, light mode default, gradient page headers available, sidebar restyled, sign-in page branded. All existing pages inherit the new color system via CSS variables.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-and-polish/08-03-SUMMARY.md`
</output>
