---
phase: 08-deploy-and-polish
plan: 05
type: execute
wave: 2
depends_on:
  - 08-03
files_modified:
  - internship-command-center/src/app/manifest.ts
  - internship-command-center/public/sw.js
  - internship-command-center/src/app/layout.tsx
  - internship-command-center/src/app/opengraph-image.png
  - internship-command-center/public/favicon.ico
  - internship-command-center/public/favicon-16x16.png
  - internship-command-center/public/favicon-32x32.png
  - internship-command-center/public/apple-touch-icon.png
  - internship-command-center/public/icon-192.png
  - internship-command-center/public/icon-512.png
autonomous: true
requirements:
  - DEPLOY-04

must_haves:
  truths:
    - "PWA manifest serves correct app name, icons, and theme color"
    - "Service worker registers and provides basic offline fallback"
    - "OG meta tags are present with custom image for social sharing"
    - "Favicon set covers all standard sizes (ico, 16, 32, apple-touch, PWA icons)"
  artifacts:
    - path: "internship-command-center/src/app/manifest.ts"
      provides: "PWA manifest with app metadata and icons"
      contains: "Internship Command Center"
    - path: "internship-command-center/public/sw.js"
      provides: "Service worker for PWA install capability"
      contains: "addEventListener"
    - path: "internship-command-center/public/icon-192.png"
      provides: "PWA icon for home screen"
  key_links:
    - from: "src/app/layout.tsx"
      to: "public/sw.js"
      via: "Service worker registration in client-side script"
      pattern: "serviceWorker"
    - from: "src/app/manifest.ts"
      to: "public/icon-192.png"
      via: "Icon paths referenced in manifest"
      pattern: "icon-192"
---

<objective>
Add PWA support (manifest, service worker, icons) and OG meta tags for social sharing. Create favicon set and app icons with blue-violet gradient branding.

Purpose: App is installable on home screens and looks professional when shared on social media.
Output: PWA manifest, service worker, favicon set, OG image, meta tags.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deploy-and-polish/08-CONTEXT.md
@.planning/phases/08-deploy-and-polish/08-RESEARCH.md
@.planning/phases/08-deploy-and-polish/08-03-SUMMARY.md

<interfaces>
From Plan 03 (layout.tsx state after 08-03 completes):
- layout.tsx will have: ThemeProvider with defaultTheme="light", Toaster with theme="system", no forcedTheme
- This plan adds SW registration and OG metadata ON TOP of 08-03's changes
- Read layout.tsx as it exists after 08-03, do not overwrite 08-03's ThemeProvider/Toaster changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PWA manifest, service worker, and favicon/icon set</name>
  <files>
    internship-command-center/src/app/manifest.ts,
    internship-command-center/public/sw.js,
    internship-command-center/public/favicon.ico,
    internship-command-center/public/favicon-16x16.png,
    internship-command-center/public/favicon-32x32.png,
    internship-command-center/public/apple-touch-icon.png,
    internship-command-center/public/icon-192.png,
    internship-command-center/public/icon-512.png
  </files>
  <action>
    1. Create src/app/manifest.ts using Next.js MetadataRoute.Manifest:
       - name: "Internship Command Center"
       - short_name: "ICC"
       - description: "Your internship command center"
       - start_url: "/"
       - display: "standalone"
       - background_color: "#f8f7ff" (light violet-tinted white)
       - theme_color: "#7c3aed" (violet-600 equivalent)
       - icons: reference /icon-192.png (192x192) and /icon-512.png (512x512)

    2. Create public/sw.js (minimal service worker per RESEARCH.md):
       - Cache name: "icc-v1"
       - Precache: ["/", "/favicon.ico"]
       - Install: open cache, add precache URLs, skipWaiting
       - Activate: clean old caches, clients.claim
       - Fetch: network-first with cache fallback (only for GET requests)

    3. Generate favicon and icon set with blue-violet gradient:
       - Create SVG source first: a clean gradient badge/shield shape with "ICC" or a stylized command center icon, using the blue-violet gradient (from ~#7c3aed to ~#6366f1)
       - Use the SVG to generate all sizes. If sharp/canvas are not available, create the icons programmatically using Next.js ImageResponse API in a script, OR create simple solid-color PNG placeholders and note that production icons should be designed.
       - Alternatively: create a small Node script (scripts/generate-icons.ts) that uses canvas or @napi-rs/canvas to render the gradient icon at multiple sizes. If these deps aren't available, create inline SVG data URIs converted to PNG via the sharp library (already commonly available), or simply create the favicon.ico and PNG files directly.
       - Required outputs: favicon.ico (48x48), favicon-16x16.png, favicon-32x32.png, apple-touch-icon.png (180x180), icon-192.png, icon-512.png
       - If programmatic generation is impractical, create an SVG file at public/icon.svg and simple placeholder PNGs, with a comment noting they should be replaced with properly rendered icons.

    4. Replace existing src/app/favicon.ico with the new one (move from public/ or update path).
  </action>
  <verify>
    <automated>cd internship-command-center && ls public/sw.js public/icon-192.png public/icon-512.png public/apple-touch-icon.png && cat src/app/manifest.ts | grep "Internship Command Center" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>PWA manifest serves app metadata. Service worker exists in public/. Favicon and icon set cover all required sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Add OG image, meta tags, and service worker registration</name>
  <files>
    internship-command-center/src/app/layout.tsx,
    internship-command-center/src/app/opengraph-image.tsx
  </files>
  <action>
    IMPORTANT: layout.tsx has already been modified by Plan 08-03 (ThemeProvider defaultTheme="light", Toaster theme="system"). Read the file first and ADD to it — do not revert 08-03's changes.

    1. Create OG image using Next.js opengraph-image.tsx file convention:
       - Create src/app/opengraph-image.tsx using ImageResponse from next/og
       - Size: 1200x630
       - Design: gradient background (blue-violet), app name "Internship Command Center" in large white text, tagline "Your internship command center" below, clean and branded
       - Alternative: if ImageResponse is complex, create a static src/app/opengraph-image.png (1200x630) as a simple gradient with text overlay. Use the same approach as the icons -- programmatic or placeholder.
       - Export size = { width: 1200, height: 630 } and contentType = 'image/png'

    2. Update src/app/layout.tsx metadata (ADDITIVE — preserve all 08-03 changes):
       - Expand the metadata export with full Open Graph tags:
         - title: "Internship Command Center"
         - description: "Track applications, generate cover letters, manage follow-ups, and nail your internship search"
         - openGraph: { title, description, url (can be relative "/"), siteName: "Internship Command Center", type: "website" }
         - twitter: { card: "summary_large_image", title, description }
         - icons: { icon: ["/favicon-32x32.png", "/favicon-16x16.png"], apple: "/apple-touch-icon.png" }
       - Note: The opengraph-image.tsx file convention auto-adds the OG image meta tag -- no need to manually specify it in metadata.

    3. Add service worker registration in layout.tsx:
       - Add a <script> tag or inline script in <head> or <body> that registers /sw.js:
         ```
         if ('serviceWorker' in navigator) { navigator.serviceWorker.register('/sw.js'); }
         ```
       - Use Next.js Script component from next/script with strategy="afterInteractive" for better performance
       - Or add it as a small client component <ServiceWorkerRegistration /> that runs the registration in useEffect
  </action>
  <verify>
    <automated>cd internship-command-center && npm run build 2>&1 | tail -5 && ls src/app/opengraph-image.* 2>/dev/null | head -1</automated>
  </verify>
  <done>OG image generates at /opengraph-image. Meta tags include OG and Twitter cards. Service worker registers on page load. All 08-03 layout.tsx changes preserved.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- /manifest.webmanifest serves correct JSON
- /opengraph-image returns an image
- public/sw.js exists
- All icon files exist in public/
- layout.tsx still has defaultTheme="light" (08-03 change preserved)
</verification>

<success_criteria>
PWA installable from home screen. Social sharing shows branded OG image. Favicon set covers all standard sizes. Service worker provides basic offline fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-and-polish/08-05-SUMMARY.md`
</output>
