---
phase: 08-deploy-and-polish
plan: 07
type: execute
wave: 2
depends_on:
  - 08-03
files_modified:
  - internship-command-center/src/app/follow-ups/page.tsx
  - internship-command-center/src/app/contacts/page.tsx
  - internship-command-center/src/app/cover-letters/page.tsx
  - internship-command-center/src/app/applications/[id]/page.tsx
  - internship-command-center/src/components/empty-state.tsx
autonomous: true
requirements:
  - DEPLOY-04

must_haves:
  truths:
    - "Every page in the app has a gradient PageHeader with title and subtitle"
    - "Empty states across all pages use rich visuals (SVG illustrations or decorative icon compositions)"
    - "Follow-ups, contacts, cover-letters, and application detail pages match the new Vercel/Stripe aesthetic"
  artifacts:
    - path: "internship-command-center/src/app/follow-ups/page.tsx"
      provides: "Follow-ups page with PageHeader and restyled layout"
      contains: "PageHeader"
    - path: "internship-command-center/src/app/contacts/page.tsx"
      provides: "Contacts page with PageHeader and restyled layout"
      contains: "PageHeader"
    - path: "internship-command-center/src/app/cover-letters/page.tsx"
      provides: "Cover letters page with PageHeader and restyled layout"
      contains: "PageHeader"
    - path: "internship-command-center/src/app/applications/[id]/page.tsx"
      provides: "Application detail page with PageHeader and visual polish"
      contains: "PageHeader"
    - path: "internship-command-center/src/components/empty-state.tsx"
      provides: "Enhanced EmptyState component with decorative visuals"
      contains: "svg"
  key_links:
    - from: "src/app/follow-ups/page.tsx"
      to: "src/components/layout/page-header.tsx"
      via: "Imports PageHeader for gradient header"
      pattern: "PageHeader"
    - from: "src/components/empty-state.tsx"
      to: "globals.css"
      via: "Uses primary/accent CSS variables for illustrations"
      pattern: "text-primary"
---

<objective>
Apply the visual redesign to all remaining pages (follow-ups, contacts, cover-letters, application detail) by adding PageHeader gradient headers and restyling layouts. Enhance the EmptyState component with decorative SVG illustrations per CONTEXT.md locked decision: "gradient headers on each page" and "illustrations for empty states and decorative moments."

Purpose: Complete the "full visual overhaul" across every page, not just dashboard and tracker. The CONTEXT.md says "everything gets the new treatment."
Output: All pages have gradient headers, empty states are visually rich, consistent premium feel throughout.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deploy-and-polish/08-CONTEXT.md
@.planning/phases/08-deploy-and-polish/08-RESEARCH.md
@.planning/phases/08-deploy-and-polish/08-03-SUMMARY.md

<interfaces>
From Plan 03 (will exist when this runs):
```typescript
// src/components/layout/page-header.tsx
export function PageHeader({ title, subtitle, children }: {
  title: string;
  subtitle?: string;
  children?: React.ReactNode;
}): JSX.Element;
```

From src/components/empty-state.tsx (current):
```typescript
// Current EmptyState component -- needs visual upgrade with illustrations
export function EmptyState({ ... }): JSX.Element;
// Uses href+Link for server component compatibility (Phase 5 decision)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PageHeader to follow-ups, contacts, cover-letters, and detail pages</name>
  <files>
    internship-command-center/src/app/follow-ups/page.tsx,
    internship-command-center/src/app/contacts/page.tsx,
    internship-command-center/src/app/cover-letters/page.tsx,
    internship-command-center/src/app/applications/[id]/page.tsx
  </files>
  <action>
    For each page, read the current file first, then add PageHeader import and wrap existing content:

    1. Follow-ups page (src/app/follow-ups/page.tsx):
       - Import PageHeader from @/components/layout/page-header
       - Add PageHeader with title "Follow-Ups" and subtitle showing count (e.g., "N follow-ups pending")
       - Add more breathing room between sections (gap-6 or gap-8)
       - Soften card borders to match the new aesthetic (use border-border instead of hard lines)

    2. Contacts page (src/app/contacts/page.tsx):
       - Import and add PageHeader with title "Contacts" and subtitle "Your networking connections"
       - Children slot: existing "Add Contact" button moved into PageHeader as action
       - Space out contact cards with more padding

    3. Cover letters page (src/app/cover-letters/page.tsx):
       - Import and add PageHeader with title "Cover Letters" and subtitle showing count
       - Add visual polish to cover letter list items (soft shadows, gradient accents)

    4. Application detail page (src/app/applications/[id]/page.tsx):
       - Import and add PageHeader with title as company name + role, subtitle as status
       - Add gradient accent to the header based on application tier (same tier colors used elsewhere)
       - Space out sections (emails, calendar, contacts, interview prep, cover letters) with clear visual separation
       - Add subtle section dividers or card groupings

    For ALL pages: preserve existing data fetching, server component patterns, and all functionality. Only change presentation layer. Do NOT break existing imports or server action call sites.
  </action>
  <verify>
    <automated>cd internship-command-center && grep -rn "PageHeader" src/app/follow-ups/page.tsx src/app/contacts/page.tsx src/app/cover-letters/page.tsx src/app/applications/\\[id\\]/page.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 remaining pages have PageHeader gradient headers. Layouts are spaced out and match the Vercel/Stripe aesthetic. All existing functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance EmptyState component with decorative SVG illustrations</name>
  <files>
    internship-command-center/src/components/empty-state.tsx
  </files>
  <action>
    Read the current EmptyState component, then enhance it:

    1. Add inline SVG illustrations to the EmptyState component. For each empty state context, create a small decorative SVG composition:
       - Use the primary color (blue-violet) from CSS variables via currentColor or hardcoded oklch/hex values
       - Style: simple, modern line art or abstract shapes (circles, lines, dots) -- NOT complex illustrations
       - Keep SVGs small and inline (no external files) -- each ~10-20 lines of SVG markup
       - Add subtle animation: gentle float or fade-in using motion library (already installed)

    2. Create a set of illustration variants based on the empty state type/context. The component likely accepts a message or type prop. Add visual variations:
       - "no applications" -- abstract shapes suggesting a list/grid with a plus icon
       - "no follow-ups" -- checkmark circle suggesting "all done"
       - "no cover letters" -- document shape with sparkle
       - "no contacts" -- people silhouette outlines
       - Default/generic -- abstract decorative shapes

    3. Add decorative background elements: low-opacity circles/blobs behind the empty state content using the primary color at 5-10% opacity. This matches CONTEXT.md: "gradient accents + background shapes (circles, blobs)."

    4. Preserve the existing href+Link pattern for CTAs (Phase 5 decision -- EmptyState uses href+Link for server component compatibility). Do NOT change to onClick.

    5. Ensure the component still works as a server component if it currently is. If it needs client interactivity for animations, add "use client" only if necessary and keep the animation minimal.
  </action>
  <verify>
    <automated>cd internship-command-center && grep -c "svg" src/components/empty-state.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>EmptyState component has decorative SVG illustrations, background shapes, and subtle animations. Different visual variants for different contexts. Server component compatibility preserved.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- All 4 remaining pages import and render PageHeader
- EmptyState has SVG illustrations (grep for `svg` in the file)
- No existing functionality broken (data fetching, server actions, navigation)
</verification>

<success_criteria>
Every page in the app has a gradient PageHeader. Empty states are visually rich with decorative SVG illustrations and background shapes. The "full visual overhaul" is complete across the entire app, not just dashboard and tracker.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-and-polish/08-07-SUMMARY.md`
</output>
