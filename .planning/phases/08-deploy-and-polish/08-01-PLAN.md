---
phase: 08-deploy-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internship-command-center/.gitignore
  - internship-command-center/.env.example
  - internship-command-center/next.config.ts
  - internship-command-center/sentry.client.config.ts
  - internship-command-center/sentry.server.config.ts
  - internship-command-center/sentry.edge.config.ts
  - internship-command-center/src/instrumentation.ts
  - internship-command-center/src/app/global-error.tsx
  - internship-command-center/package.json
autonomous: true
requirements:
  - DEPLOY-05

must_haves:
  truths:
    - "next build succeeds with Sentry wrapping and security headers"
    - "Security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy) are configured in next.config.ts"
    - "Sentry error boundary catches unhandled errors in production"
    - ".env.example documents every required environment variable"
  artifacts:
    - path: "internship-command-center/.env.example"
      provides: "Environment variable documentation"
      contains: "TURSO_DATABASE_URL"
    - path: "internship-command-center/next.config.ts"
      provides: "Security headers + Sentry + bundle analyzer config"
      contains: "withSentryConfig"
    - path: "internship-command-center/sentry.client.config.ts"
      provides: "Sentry browser initialization"
      contains: "Sentry.init"
    - path: "internship-command-center/src/app/global-error.tsx"
      provides: "Global error boundary with Sentry capture"
      contains: "captureException"
  key_links:
    - from: "next.config.ts"
      to: "sentry.client.config.ts"
      via: "withSentryConfig wrapper"
      pattern: "withSentryConfig"
    - from: "src/instrumentation.ts"
      to: "sentry.server.config.ts"
      via: "dynamic import on NEXT_RUNTIME"
      pattern: "import.*sentry\\.server"
---

<objective>
Set up production infrastructure: install new dependencies (@sentry/nextjs, @next/bundle-analyzer), configure security headers, Sentry error monitoring, bundle analysis, create .env.example, and harden .gitignore.

Purpose: Production-ready configuration layer that deployment depends on.
Output: next.config.ts with security + Sentry, all Sentry config files, .env.example, updated .gitignore, npm audit fixes.
</objective>

<execution_context>
@/Users/armaanarora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armaanarora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deploy-and-polish/08-CONTEXT.md
@.planning/phases/08-deploy-and-polish/08-RESEARCH.md

<interfaces>
From internship-command-center/next.config.ts (current):
```typescript
import type { NextConfig } from "next";
const nextConfig: NextConfig = { reactCompiler: true };
export default nextConfig;
```

From internship-command-center/src/auth.ts:
```typescript
const ALLOWED_EMAILS = process.env.ALLOWED_EMAILS?.split(",").map((e) => e.trim()) ?? []
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, audit fix, create .env.example and update .gitignore</name>
  <files>
    internship-command-center/package.json,
    internship-command-center/.gitignore,
    internship-command-center/.env.example
  </files>
  <action>
    1. cd into internship-command-center and run:
       npm install @sentry/nextjs @next/bundle-analyzer
       npm audit fix (skip major version bumps -- use --force only if needed for non-breaking fixes)

    2. Update .gitignore to be extra-cautious per CONTEXT.md. Ensure these entries exist (add if missing):
       node_modules/, .next/, .env*.local, .env, data/*.db, .DS_Store, *.sqlite, coverage/, .vercel/, .turso/, *.tsbuildinfo, .sentryclirc

    3. Create .env.example with all required vars and comments explaining where to get each one. Include:
       - TURSO_DATABASE_URL (local: file:./data/internship.db, production: libsql://...)
       - TURSO_AUTH_TOKEN (not needed for local file: URLs)
       - AUTH_SECRET (generate with: npx auth secret)
       - AUTH_GOOGLE_ID (Google Cloud Console > Credentials > OAuth 2.0)
       - AUTH_GOOGLE_SECRET (same location)
       - ALLOWED_EMAILS (comma-separated)
       - AUTH_TRUST_HOST=true (set when behind proxy like Vercel)
       - ANTHROPIC_API_KEY (Anthropic dashboard)
       - TAVILY_API_KEY (Tavily dashboard)
       - NEXT_PUBLIC_SENTRY_DSN (optional, Sentry project settings)
       - SENTRY_DSN (optional, same DSN)
       - SENTRY_AUTH_TOKEN (optional, Sentry org settings > Auth Tokens)
  </action>
  <verify>
    <automated>cd internship-command-center && cat .env.example | grep -c "=" && cat .gitignore | grep -c ".vercel" && npm ls @sentry/nextjs @next/bundle-analyzer --depth=0</automated>
  </verify>
  <done>.env.example has all env vars documented, .gitignore is production-cautious, new dependencies installed, npm audit fix run</done>
</task>

<task type="auto">
  <name>Task 2: Configure Sentry, security headers, and bundle analyzer in next.config.ts</name>
  <files>
    internship-command-center/next.config.ts,
    internship-command-center/sentry.client.config.ts,
    internship-command-center/sentry.server.config.ts,
    internship-command-center/sentry.edge.config.ts,
    internship-command-center/src/instrumentation.ts,
    internship-command-center/src/app/global-error.tsx
  </files>
  <action>
    1. Update next.config.ts:
       - Add security headers array: X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy: strict-origin-when-cross-origin
       - Add headers() async function returning the security headers for source "/(.*)"
       - Wrap with withSentryConfig from @sentry/nextjs (use pattern from 08-RESEARCH.md)
       - Integrate @next/bundle-analyzer: import bundleAnalyzer, create withBundleAnalyzer wrapper with enabled: process.env.ANALYZE === "true", chain both wrappers
       - Keep reactCompiler: true

    2. Create sentry.client.config.ts:
       - Import and init Sentry with dsn from process.env.NEXT_PUBLIC_SENTRY_DSN
       - tracesSampleRate: 0.1, replaysSessionSampleRate: 0, replaysOnErrorSampleRate: 1.0
       - Guard init with: if (process.env.NEXT_PUBLIC_SENTRY_DSN) before Sentry.init()

    3. Create sentry.server.config.ts:
       - Import and init Sentry with dsn from process.env.SENTRY_DSN
       - tracesSampleRate: 0.1
       - Guard with DSN check

    4. Create sentry.edge.config.ts:
       - Same as server but for edge runtime
       - Guard with DSN check

    5. Create src/instrumentation.ts:
       - export async function register() that dynamically imports server or edge config based on NEXT_RUNTIME
       - export onRequestError that captures via Sentry.captureRequestError (use pattern from 08-RESEARCH.md)

    6. Create src/app/global-error.tsx:
       - "use client" component
       - useEffect to call Sentry.captureException(error)
       - Render simple error UI with "Something went wrong!" and reset button
       - Must include <html> and <body> tags (Next.js global-error requirement)
  </action>
  <verify>
    <automated>cd internship-command-center && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>next.config.ts has security headers + Sentry + bundle analyzer. All Sentry config files exist. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds in internship-command-center
- `npx tsc --noEmit` has no new type errors
- .env.example exists with all documented vars
- .gitignore blocks .env, .vercel, data/*.db, *.sqlite
</verification>

<success_criteria>
Production configuration layer complete: security headers, Sentry monitoring, bundle analysis, env documentation, hardened .gitignore. Build still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-deploy-and-polish/08-01-SUMMARY.md`
</output>
